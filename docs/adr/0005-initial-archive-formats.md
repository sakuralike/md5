# ADR-0005：首版压缩格式范围

- 状态：临时接受，待产品确认
- 日期：2026-08-01
- 对应需求：桌面端验证、附录 B

## 决策

1. MVP 本地验证首先支持 ZIP 和 7z。
2. RAR 不进入首个可运行切片，待完成库许可证、加密版本兼容性和安全评审后决定。
3. 浏览器仅计算整个文件的 SHA-256 与 MD5，不在浏览器中解压或上传文件。
4. 测试样本必须由项目脚本生成，不提交真实来源压缩包或真实密码。

## 后果

数据模型保留格式枚举扩展能力；UI 对不支持格式给出明确说明，不承诺验证结果。
