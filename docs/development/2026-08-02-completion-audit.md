# 项目整体完成度审计

- 审计日期：2026-08-02
- 审计分支：`codex/m3-desktop-verification`
- 规格基线：`项目文档/密码侦探社项目规格说明书-v3.0.md`
- 计划基线：`项目文档/密码侦探社开发实施计划-v1.0.md`
- 审计性质：工程自检，不替代产品负责人、QA、安全/合规责任人和目标环境的正式验收

## 1. 结论摘要

项目已经越过“工程骨架”阶段，M1 认证基础、M2 Web 核心闭环和 M3 桌面验证/更新发布自动化核心均有可运行代码和测试证据。当前主要缺口已从单模块实现转向跨端 E2E、生产环境能力、社区信任管理闭环、规模性能、安全恢复与上线治理。

| 维度 | 工程估算 | 判断 |
|---|---:|---|
| 功能代码完成度 | 约 75% | 用户认证、指纹查询、贡献、揭示、反馈、桌面验证、更新发布主流程已实现；M4/M5 仍有较大范围 |
| MVP 验收完成度 | 约 60% | 自动化测试和本地容器门禁较完整，但目标浏览器、Windows 实机、超大样本、规模数据和生产密钥尚未验收 |
| 生产上线准备度 | 约 40% | 基础 CI、容器和迁移可用；安全扫描、恢复演练、监控告警、合规、预生产和灰度发布未闭环 |

按六个里程碑等权粗略折算约为 62%。里程碑实际业务权重并不相等，因此本报告使用“约 60% MVP 验收完成度”作为更保守的总体进度口径。

## 2. 审计范围与方法

本次检查覆盖：

1. 规格、实施计划、ADR、威胁模型、阶段状态和需求测试追踪矩阵。
2. API、Web、Admin、Windows Desktop、共享契约、基础设施和脚本目录。
3. 自动化测试收集结果、统一本地门禁、Alembic 迁移往返和 Docker Compose 空环境。
4. Git 分支、开放 PR、已提交 CI 证据和未关闭外部门禁。
5. 源码中的 `TODO`、`FIXME`、`NotImplemented` 等显式未完成标记及敏感信息风险。

完成度判断遵循实施计划中的 Definition of Done：代码完成但未在目标环境验收、未完成安全/运维文档或存在外部门禁时，不计为完全完成。

## 3. 里程碑完成度

| 里程碑 | 估算 | 已完成 | 主要缺口 |
|---|---:|---|---|
| M0 需求、安全与体验基线 | 约 85% | v3.0 规格、实施计划、9 份 ADR、威胁模型、追踪矩阵和可运行界面增量 | 正式域名、揭示配额、RAR、邮件、云/KMS、日志属地、政策责任人和完整原型确认 |
| M1 工程基础与认证 | 约 95% | 模块化 API、认证/会话、Redis 限流、幂等、TOTP、前端骨架、WPF 骨架、Compose 和 CI | GitHub 私有仓库套餐阻止主分支保护；正式邮件服务商未定 |
| M2 核心 Web 查询与贡献 | 约 75% | 指纹查询、候选加密/去重、幂等贡献、揭示审计、Web 本地计算、反馈状态机和积分首结算 | 浏览器查询到审计 E2E、断网/取消/大文件 E2E、1,000 万指纹 P95、生产 KMS/轮换 |
| M3 Windows 桌面验证 | 约 85% | ZIP/7z 验证矩阵、DPAPI 身份、挑战签名、证据接入、恢复 UX、后端更新通道和管理端发布工作台 | 8 GiB 物理样本、Windows 10/11 实机、正式 Authenticode、对象存储/CDN、高风险操作再认证/双人复核 |
| M4 社区信任与管理端 | 约 20% | Web 反馈、独立证据、自动晋级/隔离、状态事件和积分基础可复用 | 管理审核处置、举报、信誉、申诉、用户处置、配置版本、异常降权、失败激增告警和专项安全测试 |
| M5 稳定、合规与发布 | 约 10% | 基础 CI、容器镜像构建和本地冒烟脚本 | 完整 E2E/压测、SAST/DAST/供应链扫描、恢复/轮换演练、合规、监控值班、预生产、RC、灰度和正式放行 |

## 4. 代码与测试证据

审计开始时仓库包含 236 个 Git 跟踪文件，未在跟踪源码/文档中发现 `TODO`、`FIXME` 或 `NotImplemented` 标记。当前自动化测试共 67 个：

| 测试域 | 数量 | 主要覆盖 |
|---|---:|---|
| API 单元/集成测试 | 41 | 认证、安全门禁、档案核心、反馈状态机、桌面验证、桌面更新 |
| API 契约测试 | 2 | 共享前端契约 |
| Web Vitest | 6 | API 边界和本地指纹计算 |
| Admin Vitest | 7 | API 边界和桌面发布服务 |
| Desktop .NET 测试 | 11 | DPAPI/签名安全、ZIP/7z 样本矩阵、更新清单客户端 |
| 合计 | 67 | 当前自动化回归基线 |

`./scripts/check.ps1 -SkipInstall` 已通过，包含：

- Ruff 静态检查；
- pytest 与覆盖率门禁；
- SQLite Alembic 全量升降级往返；
- Web/Admin/共享包 TypeScript 类型检查、Vitest 和生产构建；
- Windows Desktop Release 构建和 11 个测试。

## 5. 本次发现并修复的阻塞缺陷

首次执行真实 Docker 空环境时，MySQL 8.4 在 Alembic `20260802_0006` 迁移处拒绝启动，原因是 `desktop_releases.release_notes` 的 `TEXT` 列声明了空字符串服务端默认值。SQLite 迁移门禁和仅构建镜像的容器 CI 没有暴露该数据库方言差异。

本次已采取以下措施：

1. 移除该 `TEXT` 列的服务端默认值，由应用层继续提供合成发布说明或空字符串。
2. 在 Docker MySQL 8.4 空环境重新执行全量迁移、服务健康检查、合成注册/登录和 Web/Admin 响应检查，约 0.9 分钟通过。
3. 在 GitHub Actions API 作业增加 MySQL 8.4 服务和 Alembic `upgrade → downgrade base → upgrade` 门禁，与本地 SQLite 门禁并行保留。
4. 首轮托管 MySQL 往返进一步发现降级时先删除外键依赖索引会被 InnoDB 拒绝；调整为直接删除整表，使索引与外键按数据库支持的顺序一起移除。修复后已在本地 MySQL 8.4 完成全量 `upgrade → downgrade base → upgrade`。

该问题属于“目标数据库运行时兼容性”缺陷；在 PR 尚未合入、迁移尚未进入正式环境时直接修正迁移是可接受的。若迁移已经发布到共享或生产环境，则必须新增纠正迁移而不能改写历史迁移。

## 6. Git 与交付状态

- 当前开发分支：`codex/m3-desktop-verification`。
- M2 PR #14 和 M3 PR #15 均为开放状态，尚未合入主分支。
- PR #15 已提交版本的 API、Frontend、Desktop 和 Container Images 作业通过。
- `main` 仍停留在 M1 基线，因此“开发分支功能存在”不等于“主分支已交付”。
- 私有仓库的主分支保护受当前 GitHub 套餐限制，仍是 M1 外部门禁。
- Dependabot 更新 PR 尚待分批评审，供应链更新不能直接视为已完成。

## 7. 风险排序

### P0：进入 M4 前应立即完成

1. 提交并验证本次 MySQL 迁移修复及托管 CI MySQL 往返门禁。
2. 处理 PR #14/#15 的基线关系和合并顺序，避免长期分支漂移。
3. 为 M4 定义管理员处置权限、原因码、审计字段、人工覆盖规则和对象级授权测试。

### P1：M4 主线

1. 候选审核列表、证据详情、状态时间线和隔离/恢复/拒绝闭环。
2. 举报处理、用户处置、信誉事件、积分补偿/重算和用户申诉。
3. 失败激增告警、关联证据降权、并发状态任务幂等和不可变审计。
4. 管理端 CSRF/XSS、对象级越权和批量接口资源限制测试。

### P2：M2/M3 验收债务

1. 浏览器“查询 → 揭示 → 复制 → 审计”E2E，以及断网、取消和大文件场景。
2. Windows 10/11 桌面实机 E2E、接近/超过 8 GiB 隔离样本测试。
3. 正式 Authenticode、对象存储/CDN、生产 KMS 和密钥轮换演练。
4. 1,000 万指纹规模的 P95 查询压测与优化计划。

### P3：M5 上线门禁

1. SAST、DAST、依赖和镜像扫描进入必过 CI。
2. 备份恢复、迁移回滚、Redis 丢失、Worker 重启和密钥轮换演练。
3. 监控、告警、SLO、值班、运行手册和事故模板。
4. 隐私政策、服务条款、授权/投诉删除、数据保留、预生产、RC 和灰度放行。

## 8. 下一阶段建议

下一轮应正式启动 M4 第 1 次迭代，但限定为一个可验收的管理处置垂直切片：

1. 新增举报和人工处置数据模型/迁移，定义原因码、操作者、前后状态、`request_id` 和幂等键。
2. 实现管理员候选筛选、证据详情、状态时间线、隔离/恢复/拒绝 API。
3. 在 Admin 增加审核列表和详情页，不在前端持久化候选秘密。
4. 增加普通用户越权、对象级授权、并发处置、重复任务和不可变事件测试。
5. 同步更新规格、API、数据模型、追踪矩阵、阶段状态和文档变更记录。

M4 第 1 次迭代出口建议为：管理员能对合成候选完成“筛选 → 查看证据 → 填写原因 → 隔离/恢复/拒绝 → 审计追踪”，且普通用户不可访问、重复请求不产生重复事件、候选秘密不进入日志或审计详情。

