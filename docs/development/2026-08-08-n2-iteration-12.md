# N2 第 12 次开发迭代：WP1 前端规范债务清零与总体验收

- 迭代日期：2026-08-08
- 所属工作包：WP1 前端规范债务与工程门禁
- 执行状态：第五轮已完成，WP1 本地工程出口条件已满足
- 分支：`codex/frontend-quality-gates`

## 1. 本轮目标

1. 完成 Admin 候选审核与桌面版本发布工作台的 Shadcn-Vue 组件迁移。
2. 删除最后两个业务页面的自定义样式块、硬编码十六进制颜色和原生表单控件。
3. 保持人工审核、关联证据降权、奖励校正、制品上传、发布与撤回等既有业务语义和安全边界不变。
4. 补充基础 SSR 渲染测试，将前端规范冻结基线清零并执行 WP1 总体验收。

## 2. 页面整改

### 2.1 Admin 候选审核

- `apps/admin/src/pages/CandidateModerationPage.vue` 使用 `Badge`、`Button`、`Input`、`Label`、`Select`、`Textarea` 替代原生表单控件。
- 使用 Tailwind Mobile-first 列表/详情布局、语义状态徽标和响应式时间线替代页面级 `<style>` 与硬编码颜色。
- 保留状态/指纹筛选、候选详情、关联反馈分组、动态降权、人工状态迁移、奖励调整和证据修订时间线。
- 审核说明继续明确禁止粘贴密码、令牌或个人信息，管理端仍不读取或返回候选密码材料。

### 2.2 Admin 桌面版本发布

- `apps/admin/src/pages/DesktopReleasesPage.vue` 使用 `Badge`、`Button`、`Checkbox`、`Input`、`Label`、`Select`、`Textarea` 替代原生表单控件。
- 使用 Tailwind 响应式发布表单、草稿恢复区和发布记录卡片替代页面级 `<style>` 与硬编码颜色。
- 保留通道/架构/最低版本、强制升级、SHA-256、签名记录、制品上传重试、发布和撤回流程。
- 页面继续声明 Authenticode 验签边界；SHA-256 和签名者信息必须来自受控发布流水线。

## 3. 自动化与规范基线

- 新增 `CandidateModerationPage.test.ts`，验证候选审核工作台、筛选入口、空状态和 SSR 不披露合成管理员令牌。
- 新增 `DesktopReleasesPage.test.ts`，验证受控发布工作台、安全边界、草稿创建入口、空状态和 SSR 不披露合成管理员令牌。
- `scripts/frontend-policy-baseline.json` 清空全部历史例外。
- 本轮历史欠账从 2 个 Admin 文件、60 项降至 0 个文件、0 项；WP1 累计从 11 个文件、164 项降至 0 项，新增违规为 0。

## 4. 验证记录

### 4.1 本轮专项验证

- Admin 测试：21 个测试文件、49 项测试全部通过。
- Admin lint：通过。
- Admin 类型检查：通过。
- Admin 生产构建：通过。
- 前端规范策略：冻结 0 项历史欠账，新增违规为 0。

### 4.2 统一门禁

Windows PowerShell 5.1 执行 `scripts/check.ps1 -SkipInstall` 已通过：Ruff 通过；API 92 项通过、1 个真实 Redis 条件测试跳过、覆盖率 90%；SQLite Alembic `base → head → base → head` 往返通过；共享契约 2 项通过；Web 22 项测试通过且 lint、类型检查和生产构建通过；Admin 49 项测试通过且 lint、类型检查和生产构建通过；Windows Desktop Release 构建与 11 项测试通过；Docker Compose 配置检查和 PowerShell 7 语法解析通过。

## 5. WP1 出口结论与下一轮计划

WP1 的本地工程出口条件已满足：Web/Admin lint、类型检查、测试与生产构建通过；业务 Vue 页面无冻结的自定义样式块、行内样式、硬编码十六进制颜色或原生表单控件例外；本轮重构的两张复杂页面均具备基础 SSR 渲染测试。

下一轮进入 WP2“账号申诉、案件编排与结果通知”，优先冻结状态模型、事务边界、幂等、权限和审计契约，再实施 API、Worker、Web/Admin 工作台及自动化测试。浏览器 E2E、真实通知环境、目标环境验收和生产放行仍是后续独立准入项，不以 WP1 本地工程门禁替代。
