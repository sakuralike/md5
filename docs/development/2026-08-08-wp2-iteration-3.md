# WP2 第 3 次开发迭代：原子案件处置与副作用收口

- 日期：2026-08-08
- 代码提交：`8f9e2b1`、`67cdbd3`
- 范围：案件最终处置、并发/幂等/审计、候选状态变更、账号恢复、积分与信誉补偿。

## 交付

- 新增 `POST /api/v1/admin/trust-cases/{case_id}/resolve`，要求 `expected_version`、结构化结果码和非空处置说明。
- 处置与候选状态、账号状态、奖励/信誉补偿在同一事务内完成，并写入 `trust_case_effects` 幂等账本。
- 支持举报、贡献申诉和账号申诉的最终结果映射；账号恢复只允许作用于案件服务端派生的目标账号。
- API、Admin 服务调用和后端回归测试覆盖版本冲突、幂等重放、越权负责人、状态/案件类型约束和副作用审计。

## 未关闭

真实 MySQL/Redis、浏览器 E2E、正式通知服务商送达和生产恢复演练不因本地测试通过而标记完成。
