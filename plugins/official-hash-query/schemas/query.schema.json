{
  "type": "object",
  "required": ["algorithm", "digest"],
  "properties": {
    "algorithm": {
      "type": "string",
      "title": "算法",
      "enum": ["md5", "sha1", "sha256", "sha512"]
    },
    "digest": {
      "type": "string",
      "title": "哈希摘要"
    }
  }
}
