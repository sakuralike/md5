# Web 公告图片外链真实验收实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans or superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox syntax for tracking.

**Goal:** 为 Web 公告补齐真实浏览器图片上传、图片加载和图片外链跳转验收，并同步项目追踪文档。

**Architecture:** 复用现有 Web/Admin 公告 API、Admin 页面文件上传控件和 Web 弹窗，不新增数据表。Playwright 辅助函数负责合成管理员登录、图片上传和公告发布；Admin 与 Web 测试分别覆盖后台交互和公开展示。

**Tech Stack:** Vue 3、TypeScript、FastAPI、Playwright、Vitest、pnpm。

---

## 任务 1：扩展浏览器公告测试辅助函数

**文件：**
- 修改：`tests/e2e/support/web_announcements.ts`
- 测试：`tests/e2e/web-announcements.spec.ts`

- [ ] 增加合成 1x1 PNG 的 `Buffer` 常量和图片上传函数，使用 `POST /api/v1/admin/web-announcements/images`、`Content-Type: image/png`，断言 HTTP 201、资源 URL 和 SHA-256 字段。
- [ ] 将 `publishWebAnnouncementForBrowser` 增加可选配置：`withImage`、`actionUrl`、`actionLabel`；默认行为保持现有调用兼容。
- [ ] 修正清理弹窗关闭状态使用的 localStorage 前缀，使其与 `AnnouncementPopup.vue` 的 `web-popup-announcement:` 一致。
- [ ] 运行 `pnpm exec tsc -p tsconfig.e2e.json --noEmit`，确认辅助函数类型通过。

## 任务 2：先写 Admin 图片上传失败测试并确认失败

**文件：**
- 修改：`tests/e2e/admin-web-announcements.spec.ts`

- [ ] 新增真实页面旅程：打开 Admin Web 公告管理，使用 `#announcement-images` 选择合成 PNG，断言上传成功提示、预览图片、填写图片点击链接、保存草稿并发布。
- [ ] 新增一个非 HTTP(S) 外链校验旅程，填写 `javascript:alert(1)`，点击保存，断言显示“跳转地址必须使用 HTTP(S)”且不产生发布请求。
- [ ] 先运行 `pnpm exec playwright test tests/e2e/admin-web-announcements.spec.ts --project=admin-chromium`，若新断言暴露实现缺陷，记录实际失败原因后再修改最小生产代码。

## 任务 3：实现 Admin 最小修复并验证绿色

**文件：**
- 可能修改：`apps/admin/src/pages/WebAnnouncementsPage.vue` 或 `apps/admin/src/services/webAnnouncements.ts`
- 测试：`tests/e2e/admin-web-announcements.spec.ts`、`apps/admin/src/pages/WebAnnouncementsPage.test.ts`

- [ ] 只修复任务 2 真实失败所对应的行为，不改变已存在的公告字段和 API 契约。
- [ ] 运行同一条 Admin Chromium E2E，确认图片上传、预览、保存、发布和非法链接保护全部通过。
- [ ] 运行 `pnpm --filter @password-detective/admin test` 和 `pnpm --filter @password-detective/admin typecheck`。

## 任务 4：补齐 Web 图片加载和外链跳转测试

**文件：**
- 修改：`tests/e2e/web-announcements.spec.ts`
- 可能修改：`apps/web/src/components/AnnouncementPopup.vue`
- 测试：`apps/web/src/components/AnnouncementPopup.test.ts`

- [ ] 使用辅助函数发布带图片、带本地 HTTP(S) 外链的公告。
- [ ] 打开 Web 首页，断言弹窗显示，图片 `src` 指向 Web 公告资源路径，且 `naturalWidth > 0`。
- [ ] 点击图片并等待新页面，断言新页面 URL 等于配置的本地外链；断言原页面没有浏览器错误。
- [ ] 增加无外链图片只展示、不生成链接的组件或浏览器断言。
- [ ] 若实现缺陷导致测试失败，只提交保持现有安全策略的最小修复。
- [ ] 运行 `pnpm exec playwright test tests/e2e/web-announcements.spec.ts --project=web-chromium` 和 Web 组件测试。

## 任务 5：同步项目文档和验收矩阵

**文件：**
- 修改：`项目文档/密码侦探社后续开发计划-2026-08-13.md`
- 修改：`docs/testing/requirements-traceability.md`
- 修改：`项目文档/文档变更记录.md`
- 新增：`项目文档/2026-08-15-web公告图片外链验收设计.md`
- 新增：`项目文档/2026-08-15-web公告图片外链验收计划.md`

- [ ] 将 R3 第 4 轮中的 Firefox/WebKit 状态改为已完成，并注明真实图片/外链旅程纳入本轮。
- [ ] 将 Web 公告追踪项的“浏览器 localStorage、图片和外链真实点击”更新为实际证据。
- [ ] 明确目标环境 SMTP、Redis、多实例、Windows Release 和正式回滚仍未完成，避免把本轮浏览器证据误写成项目整体完成。
- [ ] 运行 Markdown 链接和 UTF-8 读取检查。

## 任务 6：完整门禁、提交、推送与 Staging

**文件：**
- 不新增业务文件；提交任务 1～5 的变更。

- [ ] 运行 `pnpm lint`、`pnpm typecheck`、`pnpm test`。
- [ ] 运行 `pnpm e2e:cross-browser`，记录 Chromium、Firefox、WebKit 的实际通过数。
- [ ] 运行 `pwsh ./scripts/check.ps1 -SkipInstall`，确认统一门禁退出码为 0。
- [ ] 检查 `git status --short`，确认只包含本轮文件和用户既有保护文件 `AGENTS.md`、`易语言桌面ui.e`。
- [ ] 提交并推送当前分支；若普通 Git push 失败，使用仓库既有 Git Data API 同树同步脚本，并记录远端 SHA。
- [ ] 部署到 Staging，验证迁移、API 公开 Web 公告、Admin/Web 页面、图片资源 HTTP 200，并记录部署 revision。
