# 2026-08-17 隔离恢复演练记录

> 记录类型：本地隔离恢复验证
> 执行时间：2026-08-17 11:47:52 至 11:50:48（UTC）
> 证据状态：通过（3/3）

## 1. 演练范围与隔离措施

本次演练执行 `pwsh ./scripts/recovery-drill.ps1`，使用独立 Docker Compose 项目和合成测试数据，对以下恢复路径进行可重复验证：

- Redis 不可用期间的存活、就绪和限流降级行为；
- Worker 重启后的任务幂等性与就绪事件唯一性；
- MySQL 备份后的清库与恢复。

演练证据保存在本地忽略目录 `.local/recovery-wp4-iteration-6/`，不作为版本库内容提交；文档仅保留脱敏的结果、时序指标和校验信息。该目录使用隔离容器、卷、项目名和端口，不连接 Staging 或生产环境。

## 2. 演练结果

| 恢复场景 | 关键验证结果 | 指标 | 结论 |
| --- | --- | --- | --- |
| Redis 故障恢复 | 故障期间存活检查为 `200`、就绪检查与限流检查按预期降级为 `503`；恢复后就绪检查回到 `200` | RTO：8.852 秒 | 通过 |
| Worker 重启与幂等性 | 任务由 `pending` 进入 `ready`，Worker `ping` 返回 `pong`，`ready` 事件数为 1 | RTO：28.629 秒 | 通过 |
| MySQL 备份恢复 | 清库后目标表记录数为 0；恢复后标记数据存在 | RPO：0.828 秒；RTO：23.241 秒 | 通过 |

MySQL 备份文件 SHA-256：`d86bdc622549bf682c1a06faae53c8ad190177824438b4a0fc6248ac7c17d32f`。

## 3. 复现与证据校验

在项目根目录执行：

```powershell
pwsh ./scripts/recovery-drill.ps1
& .\apps\api\.venv\Scripts\python.exe .\scripts\verify_recovery_evidence.py --report .\.local\recovery-wp4-iteration-6\recovery-report.json --write-checksums
```

第二条命令会验证恢复报告的结构、场景结果和时间指标，并为报告写入校验和文件。2026-08-17 本次执行的验证结果为 `Recovery evidence contract passed`。

## 4. 验收边界

本记录证明的是**本地、隔离 Docker 演练**中三个恢复场景的脚本契约和实际时序，不构成以下事项的证明：

- Staging 或生产环境的 MySQL/Redis 高可用切换；
- 托管数据库、缓存或网络故障下的真实 RTO/RPO；
- 公网入口、负载均衡、监控告警与人工回滚的联动演练；
- 生产发布审批或灾备验收签字。

因此，目标环境高可用、回滚和正式 RTO/RPO 仍须在获准的目标环境窗口内按照对应运行手册补充证据，不得以本地结果替代。

## 5. 后续动作

1. 在获准的 Staging 窗口使用正式编排组合执行恢复与拓扑复核；
2. 记录目标环境的服务副本数、迁移版本、HTTP 健康检查、告警与恢复时序；
3. 将目标环境演练证据与本记录分开归档，并明确其环境、时间和验收边界。
