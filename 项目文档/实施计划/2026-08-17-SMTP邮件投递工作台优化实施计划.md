# SMTP 邮件投递工作台优化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans or superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 参考 Zibll Email 设置，将 SMTP 邮件投递升级为可直接保存并立即生效的完整管理工作台，同时保证授权码加密、脱敏和审计。

**Architecture:** 在现有 `system_settings` JSON 设置表中增加 `smtp_email_delivery` 当前配置；数据库配置优先，缺失时回退环境变量。API 请求和 Worker 任务通过同一个解析器按需构造邮件 Gateway，避免固定启动时配置导致保存后必须重启。授权码使用部署密钥环派生的 AES-GCM 密文保存，响应只返回状态。

**Tech Stack:** FastAPI、Pydantic、SQLAlchemy、Alembic、Python cryptography AES-GCM、Vue 3 Composition API、TypeScript、Shadcn-Vue、Tailwind CSS、Vitest、pnpm、PowerShell、Docker Compose。

---

## 文件结构与职责

| 路径 | 操作 | 职责 |
| --- | --- | --- |
| `apps/api/src/password_detective/core/settings_secrets.py` | 新增 | 提供配置授权码的 AES-GCM 加密、解密和密钥版本处理。 |
| `apps/api/src/password_detective/modules/admin/email_delivery.py` | 修改 | 读取环境/数据库合并配置、校验保存请求、加密授权码、构建响应和审计摘要。 |
| `apps/api/src/password_detective/modules/admin/setting_schemas.py` | 修改 | 增加完整 SMTP 保存请求、脱敏响应和 HTML/连接参数校验。 |
| `apps/api/src/password_detective/modules/admin/router.py` | 修改 | 增加 `PUT /settings/email-delivery` 路由并注入数据库会话。 |
| `apps/api/src/password_detective/core/notifications.py` | 修改 | 支持动态主题前缀、底部文本、HTML 双格式和危险内容净化。 |
| `apps/api/src/password_detective/modules/auth/context.py` | 修改 | 为每个 API 请求从当前数据库配置解析 Notification Gateway。 |
| `apps/api/src/password_detective/worker.py` | 修改 | Worker 每次投递任务从当前数据库配置构造 Gateway。 |
| `apps/api/alembic/versions/20260817_0048_smtp_email_delivery.py` | 新增 | 仅在需要记录设置键元数据时迁移；优先复用现有 JSON 设置表，不新增明文密码列。 |
| `apps/api/tests/test_settings_secrets.py` | 新增 | 覆盖 AES-GCM、nonce、密钥版本和错误脱敏。 |
| `apps/api/tests/test_admin_email_delivery_settings.py` | 新增/修改 | 覆盖 GET、PUT、权限、校验、保留/清除授权码、审计不泄密和立即生效。 |
| `apps/api/tests/test_notification_smtp.py` | 修改 | 覆盖可配置主题、双格式正文、footer HTML 净化和密码不泄漏。 |
| `apps/api/tests/test_worker_email_delivery.py` | 新增 | 覆盖 Worker 从当前数据库配置读取 Gateway。 |
| `apps/admin/src/services/settings.ts` | 修改 | 增加读取、保存完整邮件配置的类型安全 API。 |
| `apps/admin/src/pages/SystemSettingsPage.vue` | 修改 | 按 Zibll 分组实现基础邮件、底部内容、SMTP 参数、状态与测试邮件表单。 |
| `apps/admin/src/pages/SystemSettingsPage.test.ts` | 修改 | 覆盖完整字段、保存/重置、授权码脱敏和测试反馈。 |
| `packages/api-contract/src/index.ts` | 修改 | 同步完整 SMTP 保存和响应契约。 |
| `packages/api-contract/src/index.test.ts` | 修改 | 防止敏感字段进入响应类型并验证保存请求字段。 |
| `项目文档/密码侦探社项目规格说明书-v3.0.md` | 修改 | 补充 SMTP 邮件投递工作台的配置、安全和验收要求。 |
| `项目文档/设计/2026-08-17-SMTP邮件投递工作台优化设计.md` | 新增 | 记录已确认架构和边界。 |
| `项目文档/实施计划/2026-08-17-SMTP邮件投递工作台优化实施计划.md` | 新增 | 记录项目实施任务和验证路径。 |
| `项目文档/文档变更记录.md` | 修改 | 记录需求、API、数据、安全、推送和 Staging 证据。 |

## Task 1：先写配置密文失败测试

**Files:**
- Create: `apps/api/tests/test_settings_secrets.py`
- Create: `apps/api/src/password_detective/core/settings_secrets.py`

- [ ] 写测试：同一明文每次生成不同 nonce；可使用当前密钥版本解密；密钥版本不匹配和篡改密文只抛出脱敏应用错误。
- [ ] 运行 `pytest apps/api/tests/test_settings_secrets.py -q`，确认因模块不存在而失败。
- [ ] 实现最小 AES-GCM vault：密钥由 `app_secret_key` 或现有 `candidate_secret_keyring` 派生，关联数据绑定 key version 和固定用途字符串。
- [ ] 重跑定向测试，确认通过；再覆盖空值、长度和错误消息不包含明文。
- [ ] 提交 `feat(api): add encrypted settings secret vault`。

## Task 2：扩展 API 模型和邮件配置解析

**Files:**
- Modify: `apps/api/src/password_detective/modules/admin/setting_schemas.py`
- Modify: `apps/api/src/password_detective/modules/admin/email_delivery.py`
- Modify: `apps/api/src/password_detective/modules/admin/router.py`
- Modify: `apps/api/src/password_detective/modules/auth/context.py`
- Test: `apps/api/tests/test_admin_email_delivery_settings.py`

- [ ] 先写 PUT 保存、GET 脱敏、授权码留空保持原值和显式清除的失败测试，并断言审计详情不含授权码。
- [ ] 运行 `pytest apps/api/tests/test_admin_email_delivery_settings.py -q` 确认按预期失败。
- [ ] 增加 `EmailDeliverySettingsUpdate` 和经过校验的内部 SMTP 配置模型；响应只保留 `smtp_password_configured`，禁止任何原始密码字段。
- [ ] 实现环境默认值与 `SystemSetting(key="smtp_email_delivery")` 合并；首次保存写入 JSON 密文；更新时保留或清除密文。
- [ ] 增加幂等 `PUT /api/v1/admin/settings/email-delivery`，复用当前管理员权限和审计写入。
- [ ] 修改 `get_notification_gateway` 使其使用请求数据库会话和最新解析配置；保留测试中显式注入的 Gateway 优先级。
- [ ] 通过 API 定向测试，确认保存后 GET 立即反映最新配置。
- [ ] 提交 `feat(api): persist smtp delivery settings securely`。

## Task 3：实现可配置邮件主题、底部内容和 HTML 双格式

**Files:**
- Modify: `apps/api/src/password_detective/core/notifications.py`
- Modify: `apps/api/src/password_detective/modules/admin/email_delivery.py`
- Modify: `apps/api/tests/test_notification_smtp.py`

- [ ] 先写失败测试：主题使用保存的前缀；MIME 同时有纯文本和 HTML；危险标签/属性/协议被清除；授权码不出现在 MIME。
- [ ] 运行 `pytest apps/api/tests/test_notification_smtp.py -q` 确认新增断言失败。
- [ ] 为 SMTP Gateway 增加不可变的邮件品牌配置参数；普通文本通知保持原业务正文和最小披露规则。
- [ ] 实现受限 HTML 净化和纯文本降级，不添加外部依赖除非项目已有依赖无法满足；禁止脚本、事件属性、危险 URL。
- [ ] 将测试邮件、账户令牌、风险告警、信任工单和社区摘要统一使用配置品牌。
- [ ] 运行 SMTP 全套测试与相关通知测试，确认通过。
- [ ] 提交 `feat(api): support branded multipart smtp email`。

## Task 4：让 Worker 使用最新数据库配置

**Files:**
- Modify: `apps/api/src/password_detective/worker.py`
- Create: `apps/api/tests/test_worker_email_delivery.py`

- [ ] 写失败测试：数据库保存配置后，风险告警、社区摘要和信任工单 Worker 构造的 Gateway 使用该配置，而不是启动环境默认值。
- [ ] 运行 Worker 定向测试确认失败。
- [ ] 在每个邮件投递任务打开数据库会话后调用共享解析器构造 Gateway；任务结束后释放数据库资源。
- [ ] 运行 Worker 定向测试及通知投递回归测试。
- [ ] 提交 `fix(worker): reload smtp settings per delivery task`。

## Task 5：按 Zibll 结构实现 Admin 工作台

**Files:**
- Modify: `packages/api-contract/src/index.ts`
- Modify: `packages/api-contract/src/index.test.ts`
- Modify: `apps/admin/src/services/settings.ts`
- Modify: `apps/admin/src/pages/SystemSettingsPage.vue`
- Modify: `apps/admin/src/pages/SystemSettingsPage.test.ts`

- [ ] 先更新渲染测试，要求页面出现发件人昵称、标题前缀、两段底部内容、SMTP 开关、服务器、端口、Auth、用户名、授权码、加密方式、超时和测试邮件入口。
- [ ] 运行 `pnpm --dir apps/admin test -- SystemSettingsPage.test.ts` 确认新增断言失败。
- [ ] 更新共享契约和服务函数，保存请求不在浏览器状态或响应中保留服务端返回的授权码。
- [ ] 使用现有 Shadcn-Vue `Input`、`Textarea`、`Switch`、`Label`、`Button`，仅使用 Tailwind 工具类；不修改 `src/components/ui/` 自动生成源码。
- [ ] 实现授权码占位、显式清除开关、配置来源/状态卡片、字段错误和测试发送状态。
- [ ] 保存成功后刷新邮件配置与当前设置基线；重置恢复最近一次加载/保存的邮件配置。
- [ ] 运行 Admin 定向测试、类型检查、Lint 和构建。
- [ ] 提交 `feat(admin): add full smtp delivery workbench`。

## Task 6：文档、门禁和发布

**Files:**
- Modify: `项目文档/密码侦探社项目规格说明书-v3.0.md`
- Modify: `项目文档/文档变更记录.md`
- Modify: `项目文档/设计/2026-08-17-SMTP邮件投递工作台优化设计.md`
- Modify: `项目文档/实施计划/2026-08-17-SMTP邮件投递工作台优化实施计划.md`

- [ ] 回填实施状态、API 变化、加密字段、测试结果和非目标；文档示例只使用合成值。
- [ ] 运行 API、Admin、契约和 Worker 定向测试。
- [ ] 运行统一门禁：`pwsh -NoLogo -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1 -SkipInstall`。
- [ ] 运行 `pnpm --filter @password-detective/admin typecheck`、`pnpm --filter @password-detective/admin lint` 和相关构建。
- [ ] 检查 `git diff --check`、`git status` 和敏感值扫描，确认只包含本轮文件。
- [ ] 使用 `C:\Users\39859\.ssh\id_ed25519` 通过 `ssh-release` 推送 `codex/smtp-delivery-workbench` 和主线集成分支，禁止使用普通 GitHub Push API。
- [ ] 读取 `E:\只比主题\minimax\服务器md5` 中服务器配置，按受控发布脚本执行 Staging 部署；部署前备份，部署后复核 revision、API/Admin/Web HTTP、Worker、Alembic 和配置脱敏。
- [ ] 追加 `项目文档/文档变更记录.md` 发布证据，明确本地、远端和 Staging 三层结果。

## 计划自检

- 完整后台编辑由 Task 2 和 Task 5 覆盖。
- 授权码安全由 Task 1、Task 2、Task 3 覆盖，包含数据库、接口、日志、邮件 MIME 四个边界。
- 保存立即生效由 API 请求级解析和 Worker 任务级解析共同覆盖。
- Zibll 参考的品牌、两段底部内容、SMTP 参数和测试邮件入口由 Task 3 和 Task 5 覆盖。
- 文档唯一 Markdown 来源、文档变更记录、统一门禁、SSH 推送和 Staging 部署由 Task 6 覆盖。
