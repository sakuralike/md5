# WP5-I11 依赖与运行环境升级实施计划

> **执行说明：** 按步骤实施，先修复供应链高危依赖，再升级同主版本依赖，并在发布前完成锁文件、静态检查、测试、构建与 Staging 验证。

**目标：** 修复前端锁文件中的高危 `nanoid` 解析版本，并将可低风险升级的前端依赖、pnpm 与本地/CI 运行时版本基线显式统一。

**范围与原则：**

- 仅升级已验证兼容的同主版本依赖；不将 Tailwind 4、Vite 8、Vitest 4、Pinia 4、Vue Router 5、TypeScript 7 等破坏性主版本迁移混入本轮。
- Python API 继续以 `>=3.12` 与既有上界约束为兼容范围；部署镜像统一为 Python 3.12.13。
- Node.js 统一为 22.23.1，pnpm 统一为 11.19.0；Docker、GitHub Actions 与开发者版本文件以相同基线表达。
- 更新后的锁文件必须消除 `nanoid@<3.3.18` 的高危审计结果。

---

### 任务 1：修复锁文件供应链告警

**文件：**

- 修改：`package.json`
- 修改：`pnpm-lock.yaml`

- [ ] 将 `pnpm-workspace.yaml` 的 `overrides` 中 `nanoid@<3.3.17` 的覆盖版本提高至 `3.3.18`。
- [ ] 使用 `pnpm install --lockfile-only` 重新解析锁文件。
- [x] 执行 `pnpm why nanoid -r` 与 `pnpm audit --prod --audit-level=high`，预期不再解析到受影响版本且高危告警为零。

### 任务 2：升级同主版本前端依赖

**文件：**

- 修改：`package.json`
- 修改：`apps/admin/package.json`
- 修改：`apps/web/package.json`
- 修改：`pnpm-lock.yaml`

- [x] 升级 `@axe-core/playwright`、`eslint`、`globals`、`postcss`、`shadcn-vue`、`typescript-eslint` 至当前同主版本修订。
- [x] 升级 Vue 3.5 生态的 `vue`、`@vue/server-renderer`、`@lucide/vue`、`reka-ui`、`vue-component-type-helpers` 至当前同主版本修订。
- [x] 不升级跨主版本依赖；其兼容性改造另立任务。
- [x] 执行 `pnpm install --frozen-lockfile`、`pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build`。

### 任务 3：统一并显式锁定运行环境基线

**文件：**

- 修改：`package.json`
- 新增：`.node-version`
- 新增：`.python-version`
- 修改：`.github/workflows/ci.yml`
- 修改：`项目文档/密码侦探社项目规格说明书-v3.0.md`
- 修改：`项目文档/文档变更记录.md`

- [x] 将根 `packageManager` 更新为 `pnpm@11.19.0`。
- [x] 新增版本文件，分别写入 Node.js `22.23.1` 与 Python `3.12.13`。
- [x] 将 CI 中零散的 Node/Python/pnpm 版本收敛到可复用的工作流环境变量，保持与 Docker 基线一致。
- [x] 在有效规格记录可支持的 Node、pnpm、Python、MySQL、Redis 与 Nginx 基线，并写入本轮变更记录。

### 任务 4：发布与 Staging 验证

- [ ] 执行 API 静态检查、迁移往返及测试分片；执行前端完整门禁。
- [x] 创建原子提交，检查 `git diff --check`。
- [x] 通过 GitHub Git Data API 创建等价树远端提交，不使用 `git push`。
- [x] 将归档部署至 Staging，保留回滚副本，验证 API 2、Worker 3、Scheduler、Web、Admin、Alembic head 与 HTTP 健康状态。

## 验收标准

- `pnpm audit --prod --audit-level=high` 无高危漏洞。
- 新锁文件不再包含 `nanoid@3.3.17`。
- 前端 lint、类型检查、测试与构建通过；API 静态检查、迁移往返与测试通过。
- CI、Docker 与本地版本文件的 Node/Python/pnpm 基线一致。
- Git Data API 远端树与本地发布提交树一致，Staging 运行本次修订镜像。

## 实施结果（2026-08-17）

- 本地原子提交 `4a34ff800b3be3daa7e930cb99373e1129e42865` 已完成；GitHub Git Data API 发布的远端提交 `eec2878ad2975584eb7411e3521ea5a4c20d3cee` 与本地 Tree `c1e7db8f5bfef60d5a2880929e646876c2869648` 等价（12 个变更路径）。
- Staging 已使用源码归档 SHA-256 `6d9f81447418cf8ea2f42c57bb90a14cc4c299442a37c24aa8f09a778ee76b2c` 发布，运行 API 2、Worker 3、Scheduler、Web、Admin；目标 API 容器报告 Python `3.12.13`，Alembic 为 `20260817_0047 (head)`。
- Staging 本机 API ready、Web、Admin 为 HTTP 200，未认证社区管理搜索健康接口为 HTTP 401；公网 Web/Admin 入口均为 HTTP 200。
- 本机没有 Python 3.12 API 开发环境，因此未将本地 API 静态检查或完整 API 测试记为本轮通过；此次依赖变更未修改 API Python 包约束，目标容器迁移与运行时版本已复核。
