# 桌面插件开发者升级方案未完成缺口

更新时间：2026-08-28  
适用范围：`E:/只比主题/minimax/密码侦探社` 当前插件商城、桌面端、开发者中心、Admin 审核和 PDPP SDK 实现。

## 结论

本地代码已经覆盖升级方案的主要安全主链：SemVer 递增、版本不可变、权限和命令兼容、JSON Schema v1 子集兼容、签名构建溯源、PDPP 数据迁移、AppContainer 健康检查、自动回退、stable/canary 发布记录、stable 回滚、源码审核、开发者升级提交、权限授权快照、迁移结果账本和桌面端升级发现队列。

但当前不能声明“完整发布闭环已完成”。以下项目仍属于部分实现、未完成或未验证；GitHub 构建证明绑定和迁移重试已在本地实现，真实 CI artifact、认证 Canary/UAT 和目标环境仍存在外部阻塞。

## 本轮已完成

- 桌面端注册表按插件版本持久化权限申请、平台批准、实际授予、签名指纹、风险等级和用户确认时间。
- 插件 Manifest 支持可选迁移声明，限制为幂等策略和严格 SemVer 来源版本；桌面端包体检工具与服务端包校验器均执行声明校验。
- 桌面端升级账本记录 `running`、`completed`、`not_required` 和 `failed` 状态，并保存每个迁移步骤的唯一 ID、状态和尝试次数；迁移或健康检查失败时保留旧版本并写入失败信息。
- 旧版注册表和未带迁移字段的旧插件包保持兼容。
- 固定命令输入 JSON Schema v1 子集为根 `object` 加基础字段；数组、嵌套对象和 `oneOf`/`allOf` 等组合关键字由服务端、桌面端和官方体检工具一致拒绝。
- 升级兼容检查覆盖必填字段、附加属性、枚举、格式、默认值、正则、长度、数值边界和 `multipleOf`，并补充兼容/不兼容矩阵测试。
- 桌面端打开插件市场时自动检查一次 stable 更新，也支持手动检查；分页扫描公开 catalog，并按 SemVer 数值比较已安装的平台审核插件。
- 已发现更新进入独立队列，用户可选择插件执行升级；升级复用下载摘要、开发者签名、平台签章、撤销列表、权限确认、迁移和健康检查流程，失败项保留在队列中重试。
- 已登录桌面端使用已注册安装实例的 ECDSA P-256 密钥签署脱敏升级证据；服务端验证账号、设备签名和平台版本一致性后保存权限授权、迁移步骤和结果。
- 管理员可通过 `GET /api/v1/admin/plugin-reviews/install-evidence` 分页查看设备签名证据；证据不包含私有数据、文件路径、迁移错误原文、访问令牌或插件输出。
- 桌面端提供内部 Canary 通道入口；仅管理员令牌、已注册安装实例和设备 ECDSA 签名可读取 Canary 目录、签发票据和消费下载票据，票据绑定账号/设备并受有效期限制。
- CI 增加 Windows runner 的官方皮肤双构建证明，校验 Git 提交、逐文件 provenance 摘要和工具链信息，并上传 `build-proof.json` 与 SHA-256 清单；本地已预演通过，GitHub runner 尚未实际启动。
- 本轮提交 `14fd7ab980ded6e59f5cf8b5a0ce1a536f70c590` 已推送到 `ssh-release/codex/desktop-plugin-control-plane`，远端分支与本地提交一致；用户未提交的 README 与积分商城规划文件未混入提交。
- 服务器已部署 API/Worker/Scheduler `password-detective-api:14fd7ab`、Web `password-detective-web:14fd7ab`、Admin `password-detective-admin:14fd7ab`；备份为 `/opt/password-detective-backups/20260827T200731Z-14fd7ab`，迁移为 `20260828_0063 (head)`，API 2 实例、Worker 3 实例和 Scheduler 均健康。
- 服务器公网未授权冒烟已通过：ready、stable catalog、revocations、Web、Admin 均 HTTP 200；未授权 Canary catalog 和 install-evidence 均 HTTP 401。
- 版本构建证明接口要求受信 GitHub 仓库的成功 `plugin-build-proof` artifact，并在服务端核对 CI 提交、包内 provenance 和版本架构制品摘要；源码/可复现版本发布前必须完成绑定。
- 迁移失败回执进入服务端退避队列，Celery 调度后由认证桌面轮询领取；最多三次，成功回执会关闭待重试项。
- 本轮使用服务器凭证完成 Staging SSH 部署：API/Worker/Scheduler 已切换至 `031afdf59f35`，MySQL 迁移为 `20260902_0064 (head)`；2 个 API、3 个 Worker、Scheduler 和 API proxy 健康，Web/Admin 代理冒烟通过。认证桌面迁移重试闭环、独立 Runner VM、真实 mTLS/隔离网络、UAT/Production 仍未验收。
- Staging API 通过 Compose secret 挂载只读 GitHub token 文件，entrypoint 复制到降权运行时秘密目录；真实构建证明下载验证仍需在配置完成后复验。

## 部分实现

| 缺口 | 当前状态 | 影响 | 完成标准 |
| --- | --- | --- | --- |
| 构建溯源的外部可验证性 | CI Windows runner 双构建官方皮肤并校验 Git 提交、逐文件摘要、工具链和重建内容；服务端已增加受信 artifact 读取和发布前绑定 | GitHub Actions 仍需成功运行并保留真实 `plugin-build-proof` artifact；第三方插件尚未在目标环境接入真实发布流程 | 触发成功 CI 并绑定真实 run/artifact；所有 source/reproducible 发布均通过接口门禁 |
| 数据迁移契约 | PDPP 已增加 `lifecycle/migrate`；桌面端和服务器保存设备签名、制品摘要、步骤 ID、状态和尝试次数；服务端已有退避重试调度 | 真实桌面轮询、目标环境 Worker 调度和受信发布门禁仍未验收 | 在目标环境执行失败→重试→成功/耗尽闭环并保留验收证据 |

## 未完成

| 缺口 | 当前状态 | 完成标准 |
| --- | --- | --- |
| 官方插件重新构建和发布 | 当前 HEAD 的本地双构建 provenance 证明可生成并校验；服务器现有官方插件版本早于本轮 provenance/迁移能力，尚未重新上传发布 | 使用新构建器重建官方升级包，验证 provenance、AppContainer 和迁移，再走开发者上传、自动审核、人工批准、stable 发布 |
| 发布门禁与 CI 绑定 | 发布接口已读取并验证 GitHub `plugin-build-proof` artifact；source/reproducible 版本无证明不能发布 | 真实成功 artifact、第三方插件接入和目标环境发布验收仍待完成 | 将包构建、四语言合规、源码/SBOM/provenance、升级/回退测试全部绑定发布流水线 |
| 目标环境验收 | 本轮服务器已完成运行时和未授权保护冒烟；认证 Canary、真实 Windows 安装、UAT/Production 升级/回退/撤销仍未执行。服务器当前有 `admin`（ACTIVE/ADMIN），但本地没有该账号的有效密码和 TOTP；旧凭据对应已删除的 `site_admin_86555042`，不能用于验收 | 提供现存管理员账号的有效登录与 TOTP，完成 Canary 受控下载、真实 Windows 客户端安装、升级/回退/撤销和恢复验收，并保留 UAT/Production 签署记录 |

## 已验证但不等于目标环境完成

- 本轮桌面端完整测试通过 111/111；API 插件控制面、审核流程、迁移证据与迁移头定向测试通过，API 合同测试 8/8，Python 编译和 Ruff 检查通过；PDPP SDK、跨语言合规工具、官方插件体检工具和桌面 Release 构建通过。
- 既有桌面端测试、API 插件控制面/审核定向测试、Admin/Web 定向测试、API 合同测试、TypeScript 检查、Release 构建和 C#/Go/Rust/Python AppContainer 合规已在本地通过。
- 本地通过只证明当前工作树的确定性行为；服务器已完成本轮运行时和未授权保护冒烟，但不证明真实 Windows 用户端、UAT 或 Production 已放行。
- 用户已明确后续不要求每轮执行全量统一 API 门禁；本清单只要求插件相关定向测试和发布链路证据，不把全量 API 测试列为本轮阻塞项。

## 阻塞解除后的收口顺序

1. 修复 GitHub Actions 托管 runner/账户执行条件，保留 `plugin-build-proof` artifact 和 run URL。
2. 使用有效管理员账号和已注册设备完成 Canary 受控安装，再执行 UAT/Production 升级、回退、撤销和恢复验收。
3. 重建官方插件，走上传、审核、stable 发布和真实下载摘要校验。

## 相关实现

- [桌面插件开发者升级方案](./桌面插件开发者升级方案.md)
- [插件开发指南与 SDK](../桌面插件开发指南与SDK.md)
- [API 插件包校验器](../../apps/api/src/password_detective/modules/desktop_plugins/package_verifier.py)
- [桌面端插件安装器](../../apps/desktop-windows/Plugins/Installation/PluginInstaller.cs)
- [PDPP SDK](../../packages/pdpp-sdk-dotnet/PdppPlugin.cs)
- [插件包构建器](../../tests/pdpp-package-builder/Program.cs)
