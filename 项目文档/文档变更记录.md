# 文档变更记录

## 2026-08-01

### 建立 v3.0 项目基线

- 阅读并整合项目文档目录内原有 8 份 Word 文档。
- 新建《密码侦探社项目规格说明书 v3.0》作为产品、需求、架构、安全和验收基线。
- 新建《密码侦探社开发实施计划 v1.0》，建立 14 周 MVP 实施路线。
- 统一“压缩包文件指纹 → 解压密码”的数据语义。
- 删除 `hash(password) == archive_file_hash` 的错误校验设计。
- 将桌面端可信模型由 User-Agent/硬件 ID 调整为安装密钥、挑战签名、防重放和风控证据。
- 统一候选状态为 `pending / verified / quarantined / rejected`。
- 将泄露口令爬虫、明文反查、论坛、商城、悬赏破解、AI 猜密等能力移出 MVP。

### 切换为 Markdown 唯一维护格式

- 将当前规格和开发计划移动到项目文档根目录，以 `.md` 作为唯一事实来源。
- 将原始 8 份 Word 文档转换为历史 Markdown。
- 将所有 DOCX 移动到 `历史文档/docx归档/`，不再维护。
- 新增《文档维护规范》，约定命名、版本、评审、归档、安全和变更流程。
- 更新项目文档索引和历史文档说明。

### 启动 M0/M1 分阶段模块化开发

- 创建单仓代码结构以及 API、Web、Admin、Windows Desktop、共享包、基础设施和开发文档目录。
- 新增 5 份 ADR、初始威胁模型、模块地图和需求测试追踪矩阵。
- 完成首个认证垂直切片、首批数据库迁移、会话轮换与重放检测。
- 完成 Vue 用户端/管理端骨架和 WPF 本地 SHA-256/MD5 计算 PoC。
- 新增 Docker Compose、CI 和统一质量检查脚本。
- M1 仍需完成 Redis 分布式限流、持久化幂等、邮箱验证/密码重置、TOTP 和容器环境实测。
