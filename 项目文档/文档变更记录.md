## 2026-08-10

### WP4 第 13 次迭代：资源趋势与 HA 执行证据合同

- 新增资源趋势、MySQL HA、Redis HA 三类合成报告示例和 `staging-execution-evidence-bundle-v1` 校验器，覆盖 4 小时窗口、采样覆盖、P95/错误率、CPU/内存、连接池预算、队列清零、RTO/RPO、数据完整性、幂等与回滚检查。
- 新增 `pnpm staging:evidence-contract`、`check.ps1 -IncludeStagingEvidence` 和 GitHub Actions `staging-evidence-contract` 作业，输出 profile/计划/报告/摘要/checksum 的脱敏证据包。
- profile 绑定摘要统一 CRLF/LF 后计算，消除 Windows/Linux checkout 的合同标识漂移；实际证据文件 checksum 仍按原始字节生成。
- 明确 `contract-fixture` 只表示合同校验通过，固定为 `not-run` / `pending-evidence`；真实目标环境报告必须标记 `target-execution`，校验通过后也只进入 `pending-approvals`，不自动生成 Go/No-Go。
- 同步更新 WP4 迭代记录、Staging 运行手册、本地开发指南、阶段状态、需求追踪矩阵、README、规格说明书、实施计划和下一步开发方案。
- 下一轮进入目标环境资源采集器、HA 适配器和不少于 4 小时人工触发门禁；在真实时间线与角色签字出现前不宣称 staging 或 HA 验收完成。

# 文档变更记录

## 2026-08-10

### WP4 第 16 次迭代：证据包封存与审批交接完整性门禁

- 新增 `staging-evidence-archive-manifest-v1` 封存器，重验六个主证据文件、主/来源 checksum、执行组、执行标识、profile 摘要、来源摘要和目标执行适配器边界。
- 新增确定性 `staging-evidence-archive.zip`、封存清单 detached SHA-256、ZIP detached SHA-256 和候选提交绑定；原始平台导出不进入交接包。
- 新增 `staging-evidence-archive.ps1`、`staging-evidence-archive-contract.ps1`、10 项正负向测试、`pnpm staging:evidence-archive-contract`、`check.ps1 -IncludeStagingArchive` 和独立 CI 合同作业；专项测试与统一门禁均通过。
- 固定 `contract-fixture` 为 `contract-sealed / blocked-by-contract-fixture / not-run / pending-evidence`；真实 `target-execution` 封存最高进入 `ready-for-approvals / pending-approvals`，不会自动生成签字或 Go/No-Go。
- 同步规格说明书、实施计划、运行手册、阶段状态、需求追踪矩阵、README 和下一步开发方案；下一轮在已批准 staging 上执行真实 4 小时会话、HA 切换、封存并完成四角色审批。

## 2026-08-10

### WP4 第 12 次迭代：Staging 长时稳定性准入合同

- 新增版本化 Staging 准入配置、严格校验器、7 项专项测试、PowerShell 计划生成入口和 SHA-256 清单，将 4 小时持续负载、资源采样、单 Worker 故障及操作阈值固化为机器合同。
- Compose 显式设置 Worker 并发；默认拓扑连接池预算为请求 `110`、允许 `136`、余量 `26`，配置超预算时拒绝生成执行计划。
- 新增 MySQL/Redis 高可用 RTO/RPO 证据要求、Staging 运行手册和技术/安全/运维/业务四方 Go/No-Go 模板，并递归拒绝敏感配置进入证据。
- 新增 `pnpm staging:readiness-plan`、统一门禁 `-IncludeStagingReadiness` 和 GitHub Actions `staging-readiness-contract` 作业。
- 同步更新 v3.0 规格、实施计划、下一步开发方案、阶段状态、追踪矩阵、README 与本地开发指南。
- 当前结果仅为 `contract-valid` 静态合同；实际 4 小时 staging 运行、资源趋势、MySQL/Redis HA 切换和四方签字保持 `not-run` / `pending-evidence`。

## 2026-08-10

### WP4 第 11 次迭代：多实例稳定性与单 Worker 故障收口

- Worker 心跳改为实例级 TTL 键并新增聚合实例数指标，保留旧共享键读取兼容；单实例停止不再误报整个 Worker 依赖不可用。
- Compose 将 Celery Beat 拆为单独 `scheduler`，Worker 可安全横向扩展；MySQL SQLAlchemy 连接池参数纳入配置样例与 Compose。
- 新增并发 API/MySQL/Redis/Celery 探针、3 Worker 单实例故障/替补演练、证据校验器、`pnpm stability:multi-instance`、统一门禁和 CI 作业。
- 远端 CI 首轮暴露补 Worker 时 Compose 连带重建 API 的问题；恢复步骤改为删除故障容器后使用 `--no-deps --no-recreate` 仅创建缺失 Worker，避免把编排抖动误判为应用故障。
- 同步 v3.0 架构约束、实施计划、下一步开发方案、阶段状态、追踪矩阵、README、本地开发指南和运行手册。
- 当前只关闭单机 Compose 应用级多 Worker 回归；staging 长时趋势、MySQL/Redis 高可用、容量预算、生产 Secret/KMS 与目标环境签字仍为后续门禁。

## 2026-08-10

### WP4 第 10 次迭代：候选秘密密钥轮换与回滚闭环

- 候选秘密加密模型新增当前密钥写入、历史版本读取和独立稳定去重密钥；明确旧配置兼容、版本格式、密钥数量与非本地长度约束。
- 新增默认 dry-run 的数据库轮换命令、去重一致性保护、正向轮换和回滚合成演练、证据校验器与运行手册。
- 新增 `pnpm candidate-secrets:drill`、`check.ps1 -IncludeKeyRotation` 和 GitHub Actions 独立证据作业。
- 同步 v3.0 数据保护细则、威胁模型、阶段状态和下一步开发方案；生产 Secret/KMS、多实例长时稳定性和目标环境签字仍为后续门禁。

## 2026-08-10

### WP4 第 7 次迭代远端 CI 收口补丁

- 针对恢复作业中 API 容器启动失败但证据缺少容器日志的问题，恢复报告新增脱敏后的 Compose 状态、API 日志和 Worker 日志。
- API Compose 启动增加 Alembic 升级的 5 次有界重试，每次间隔 5 秒；重试耗尽仍失败退出，不放宽迁移或恢复验收标准。
- 恢复脚本新增 API、MySQL、Redis 宿主机端口预检，避免误连其他本地服务；Compose 部分启动时同样执行资源清理。
- 本地隔离恢复演练和证据校验复验通过，远端以最终提交对应的 GitHub Actions 结果为准。

## 2026-08-10

### WP4 第 6 次迭代：MySQL、Redis 与 Worker 恢复演练

- 新增独立 Compose 恢复脚本和运行手册，实际执行 MySQL 逻辑备份/清空/恢复、Redis 故障降级/恢复、Worker 中断重启与隐私导出任务幂等。
- 新增 `recovery-drill-v1` 证据校验器、3 项单元测试、`pnpm recovery:drill`、`check.ps1 -IncludeRecovery` 和 GitHub Actions `recovery` 作业。
- 修复 Worker 容器缺少可写制品目录与 Celery Beat schedule 权限问题；Compose 对外端口支持隔离配置，新增 `.dockerignore`。
- 本地真实演练通过：MySQL RPO 0.648 秒/RTO 17.648 秒、Redis RTO 8.908 秒、Worker RTO 31.183 秒，重复任务就绪审计事件为 1 条。
- 同步下一步开发方案、阶段状态、需求追踪矩阵、README 和本地开发指南；下一轮进入性能基线与可观测性出口。

## 2026-08-10

### WP4 第 5 次迭代：管理员 MFA、再认证与幂等滥用门禁

- 新增管理员 MFA/再认证动态滥用探针：覆盖初始 MFA 阻断、TOTP 确认、可信 Origin 登录、错误当前密码拒绝和无缓存安全响应。
- 新增管理员状态变更幂等精确重放、同 key 冲突和一次性再认证授权重放拒绝门禁；`rate_limit.exceeded` 统一补充 `Retry-After` 响应头。
- DAST 固定检查由 10 项扩展为 13 项；定向测试 20 项通过、1 项真实 Redis 条件测试跳过，本地 `13/13` 通过，报告不保存密码、TOTP、令牌或 Cookie。
- 完成阶段状态、动态安全基线、本地开发指南、需求追踪矩阵、README 和下一步开发方案同步；本地提交 `eb9fa79`，远端等价提交 `4a96db42`，CI `31352246919` 九个作业全部通过。
- 管理员认证滥用动态证据已关闭；恢复、性能、可观测性、完整 BOLA、人工渗透和目标环境验收继续列为后续范围。

## 2026-08-10

### WP4 第 4 次迭代：认证对象边界与浏览器 Cookie 安全门禁

- 记录普通用户隐私导出读取、会话撤销的跨用户对象级授权边界。
- 记录浏览器刷新 Cookie 的 HttpOnly、SameSite=Lax、路径隔离和 Origin 校验要求。
- 更新动态 DAST 基线为 10 项，并记录本地统一门禁、API 全量测试和远端 CI 证据。
- 明确管理员 MFA/再认证、幂等重放、资源消耗、恢复、性能和可观测性为后续开发范围。


## 2026-08-09

### WP4 第 3 次迭代：API 动态安全与负向门禁

- 新增应用层 API 安全响应头和 `MAX_JSON_BODY_BYTES`：JSON 请求体默认限制 1 MiB，并按实际接收字节阻断分块传输绕过；桌面二进制制品继续使用独立流式限制。
- 新增 7 项 API 负向安全测试，覆盖正常/401/405 响应头、CORS 白名单、超大 JSON、XSS/凭据不反射和匿名用户/管理边界。
- 新增 `pnpm security:dast`、隔离 API 启动脚本和 8 项确定性黑盒探针；`check.ps1 -IncludeSecurity` 同时执行静态与动态安全门禁。
- GitHub Actions 新增 `dast-security` 作业和 30 天提交级证据；首次运行 `31339839339` 暴露 Linux Ruff 可执行位规则后移除无必要 shebang，提交 `45af0d3` 的运行 `31340260242` 九个作业全部通过。
- 远端 DAST 制品已下载复核，报告 `total=8`、`passed=8`、`failed=0`；本地统一门禁 109 项 API 通过、1 项条件跳过、覆盖率 90.20%，11 项安全脚本测试和 8/8 动态探针通过。
- 新增 API 动态安全基线和 WP4 第 3 次迭代记录，同步 v3.0 规格、README、本地开发指南、阶段状态、追踪矩阵和下一步开发方案。认证 BOLA、Cookie CSRF、MFA/再认证与幂等重放、资源消耗、人工渗透、恢复、性能和可观测性仍未关闭。

### WP4 第 2 次迭代：镜像、Secret 与限期风险豁免门禁

- 在 `container-images` 作业中加载 API/Web/Admin 三个最终镜像，使用固定完整提交 SHA 的 Trivy Action 和明确的 Trivy `v0.73.0` 生成独立 HIGH/CRITICAL JSON 报告；新增仓库 Secret 扫描和提交级发布安全证据制品。
- 新增 `verify_release_security.py`、`security/risk-acceptances.json` 和 5 项合成单元测试；风险接受必须精确匹配作用域/Finding/严重级别，并具备不同负责人/批准人、工单、原因和未来到期日期，过期或重复记录自动阻断。
- 首次 GitHub Actions `31331415752` 正确阻断旧 Python/Nginx 基础镜像风险，仓库 Secret 为 0；未新增豁免，改为更新 Python 3.12.13 Alpine 3.23、Node 22.23.1 Alpine 3.23 和 Nginx 1.30.4 Alpine 3.24，并执行系统包安全更新。
- 整改提交 `07abd25` 对应远端 GitHub Actions `31331945963` 八个作业全部通过；三个镜像高危/严重漏洞 0、仓库 Secret 0、风险接受 0，远端制品已下载并由本地校验器二次复核。
- 本地 `check.ps1 -SkipInstall -IncludeSecurity`、9 项安全校验器测试和仓库 Secret 实扫通过；本机 Docker Desktop Linux 引擎未运行，因此镜像实扫证据明确来自远端 Linux CI。
- 新增发布镜像/Secret/风险接受门禁和 WP4 第 2 次迭代记录，同步 README、本地开发指南、阶段状态、追踪矩阵和下一步开发方案；本地 API、Web、Admin 入口及前端 API 代理均已启动并返回 HTTP 200。DAST、专项人工安全测试、恢复、性能、可观测性和目标环境仍未关闭。

### WP4 第 1 次迭代：安全审计与 SBOM 门禁

- 新增 `security-gate.ps1`、安全证据校验器和 4 项合成单元测试；本地可通过 `pnpm security:audit` 或 `check.ps1 -IncludeSecurity` 执行 Python/Node 生产依赖审计、API Bandit SAST、API CycloneDX SBOM 和 SHA-256 清单。
- GitHub Actions 新增 `security-supply-chain` 作业，使用完整提交 SHA 固定 SBOM/上传动作，生成仓库 CycloneDX SBOM，并以 `security-evidence-<commit-sha>` 上传 30 天证据。
- 修复首次审计发现的 `nanoid` 高危依赖；将 `cryptography` 提升到 50.x 安全基线并更新 pytest 下限。
- 功能提交 `dffdffc` 对应远端 GitHub Actions `31322842317` 八个作业全部通过；远端五份 JSON 安全证据已下载并再次通过结构与阻断规则校验。
- 新增安全与供应链门禁、WP4 第 1 次迭代记录，并同步 README、本地开发指南、阶段状态、需求追踪矩阵和下一步开发方案。镜像/秘密扫描、DAST、人工安全测试、豁免治理、恢复、性能和可观测性仍未关闭。

### WP3 第 11 次迭代：Firefox 与 WebKit 跨引擎门禁

- 将 Web/Admin 完整 30 项 Playwright 旅程扩展到 Chromium、Firefox、WebKit 三个隔离引擎，本地共 90/90 通过；新增跨浏览器统一检查参数和各浏览器独立命令。
- GitHub Actions E2E 作业升级为三浏览器矩阵，每个引擎单独安装、运行和上传命名隔离的失败制品；功能提交 `8274ea4` 对应远端 GitHub Actions `31319884808` 七个作业全部通过。
- 移动响应式门禁移除 Firefox 不支持的 `isMobile` 模拟；WebKit 自动化明确采用直接聚焦跳过链接后以 Enter 验证激活，不将该结果冒充真实 Safari 系统完整键盘访问或 VoiceOver 证据。
- 新增 WP3 第 11 次迭代记录，并同步本地开发指南、浏览器矩阵、视觉基线策略、阶段状态、需求追踪矩阵和下一步开发方案。

### WP3 第 10 次迭代：剩余业务页面无障碍矩阵收口

- 扩展 Web 贡献、积分信誉、举报申诉、活动记录和 Admin 候选审核、信任案件、系统配置、风险告警的桌面/移动结构化视觉、无溢出和 Axe 严重/关键门禁；完整 Playwright Chromium 旅程由 28 项增至 30 项。
- 修复根 Tailwind 内容扫描路径、共享旧变量与语义变量冲突、候选审核页面超宽布局、Select 可辨识名称、实时反馈语义及登录/配置页面实际对比度问题；增强视觉与 Axe 失败诊断信息。
- 新增跨平台视觉基线策略、屏幕阅读器人工检查清单、浏览器与设备验收矩阵和 WP3 第 10 次迭代记录；同步更新下一步开发方案、阶段状态和需求追踪矩阵。
- 本地 `scripts/check.ps1 -SkipInstall -IncludeE2E` 统一门禁与 30/30 Chromium 旅程通过；功能提交 `10e527d` 的远端 GitHub Actions `31310591798` 五个作业全部通过。跨平台像素批准、真实读屏器、Edge/Firefox/Safari、真实设备和目标环境仍保持未验证。

### WP3 第 8 次迭代：视觉、可访问性与刷新竞争收口

- 更新 WP3 验收基线：新增 Web/Admin 首批结构化视觉快照、关键布局边界、页面级无溢出、登录表单焦点顺序、错误实时区和 Axe 严重/关键 WCAG 门禁。
- 更新身份安全验收：补充刷新 Cookie 缺失、自然过期与多标签并发竞争；服务端以条件更新原子认领未撤销会话，竞争失败保持令牌族撤销和不可变审计。
- 同步下一步开发方案、阶段状态、需求追踪矩阵和第 8 次迭代记录；当前 25 项 Chromium 旅程与 `scripts/check.ps1 -SkipInstall -IncludeE2E` 统一门禁通过，跨平台像素差异、更多核心页面、屏幕阅读器和目标环境验收仍独立关闭。
- 对应代码：Web/Admin 响应式与对比度修正、`@axe-core/playwright` 视觉/可访问性辅助、4 个首批页面门禁、刷新异常/竞争种子与 E2E，以及 Admin 顶层布局 SSR 测试。远端 GitHub Actions `31305403414` 的 API、Frontend、Desktop、E2E 和 Container Images 五个作业全部通过。

### WP3 第 1 次迭代：Playwright 基础设施与认证核心旅程

- 更新 v3.0 测试与验收基线：冻结隔离数据库、合成账号和含认证材料场景的截图/Trace 脱敏约束。
- 更新下一步开发方案、阶段状态、需求追踪矩阵、README 和迭代记录，记录 Web/Admin 认证 E2E 已实现范围与剩余核心旅程。
- 对应代码：Playwright 配置、E2E API 启动/种子脚本、Web/Admin 4 项 Chromium 测试、GitHub Actions `e2e` 作业和 TOTP 输入值绑定修复。
- 修复 `20260802_0009`、`20260804_0018` 及 `20260808_0019`～`0022` 在 MySQL 回滚时先删除外键依赖索引导致 `1553` 的问题：字段回滚先删除外键，整表回滚直接删表并由数据库原子清理约束与索引。
- 本地 `scripts/check.ps1 -SkipInstall -IncludeE2E`、`pnpm lint`、`pnpm typecheck`、`pnpm e2e` 和 SQLite 迁移往返通过；远端 GitHub Actions 运行 `31290619825` 的 API、Frontend、Desktop、E2E 和 Container Images 全部通过。期间补齐 `infra/docker/frontend.Dockerfile` 的根级 `postcss.config.js` 与 `tailwind.config.js` 构建上下文。查询/贡献/处置旅程、视觉/移动端和目标环境验收仍独立关闭。

## 2026-08-08

### WP2 第 3～4 次迭代：案件处置、通知 Outbox、SLA 与 Web 账号申诉

- 更新 v3.0 规格：案件主记录补充 `sla_due_at`、自动升级元数据和结果通知 Outbox；新增原子 `resolve`、通知列表/重放接口和案件处置副作用/幂等账本约定。
- 更新下一步开发方案、阶段状态、需求追踪矩阵和迭代记录：记录四轮开发任务的已实现项、测试证据和真实环境未关闭项。
- 对应代码：`20260808_0021`～`20260808_0023` 迁移、Trust Case 原子处置/副作用/通知/SLA 服务、Celery 任务、Admin 工作台和 Web 账号申诉表单。
- 本地针对性门禁通过：API Trust Case 回归、API Ruff、契约测试、Web/Admin lint、typecheck、Vitest 和 SSR 测试；远端推送需以 GitHub 网络可达后的实际结果为准。

### WP2 第 1 次迭代：账号申诉与统一案件主体基线

- 更新 v3.0 规格：案件类型扩展为 `report`/`appeal`/`account_appeal`，冻结 `candidate/account/risk_alert` 单一主体约束、本人账号申诉、最小披露和本人详情对象级隔离。
- 更新 API 约定：新增 `/trust/account-appeals`、`/trust/cases/{case_id}`，补充账号目标派生、限流、幂等、404 防枚举和审计披露边界。
- 更新下一步开发方案、模块地图、阶段状态、需求追踪矩阵、README，并新增 WP2 第 1 次开发迭代记录。
- 对应代码：`TrustCase` 数据模型、`20260808_0019` 迁移、trust cases API/服务/契约、Web/Admin 案件主体展示和自动化测试。
- Windows PowerShell 5.1 统一门禁通过：API 94 项通过、1 项真实 Redis 条件测试跳过、覆盖率 90%；共享契约 2 项、Web 23 项、Admin 49 项、Desktop 11 项测试，以及 Ruff、前端规范检查、类型检查、生产构建、桌面 Release 构建和 SQLite 全量迁移往返通过。

### 完成 WP1 第五轮候选审核与桌面发布页面整改及总体验收

- Admin 候选审核页迁移到 Badge、Button、Input、Label、Select、Textarea 和 Tailwind Mobile-first 布局，保留筛选、关联证据降权、人工状态迁移、奖励校正和不可变时间线。
- Admin 桌面发布页迁移到 Badge、Button、Checkbox、Input、Label、Select、Textarea 和 Tailwind 响应式布局，保留 SHA-256、签名记录、上传恢复、发布与撤回安全边界。
- 新增两项 SSR 基础渲染测试；Admin 当前 21 个测试文件、49 项测试通过。
- 前端规范冻结基线从 2 个 Admin 文件 60 项清零；WP1 累计从 11 个文件 164 项降至 0 项，新增违规为 0，本地工程出口条件已满足。
- 同步更新下一步开发方案、阶段状态、模块地图、需求追踪矩阵、README 和 N2 第 12 次迭代记录；下一轮进入 WP2 账号申诉、案件编排与结果通知。
- Windows PowerShell 5.1 统一门禁通过：API 92 项通过/1 个真实 Redis 条件测试跳过、覆盖率 90%，共享契约 2 项、Web 22 项、Admin 49 项、Desktop 11 项；迁移往返、前端 lint/typecheck/test/build、桌面 Release 构建、Compose 配置和 PowerShell 7 语法解析通过。浏览器 E2E、真实通知环境、目标环境验收和生产放行仍为独立未关闭项。

### 完成 WP1 第四轮 Admin 信任案件与风险告警页面整改

- Admin 举报/申诉案件页迁移到 Badge、Button、Input、Label、Select、Textarea 和 Tailwind Mobile-first 布局，保留案件筛选、状态迁移、幂等和不可变时间线。
- Admin 风险告警页迁移到 Badge、Button、Checkbox、Input、Label、Select、Textarea 和 Tailwind 语义危险态，保留 SLA、负责人指派、通知失败重放和处置闭环。
- 通过 Shadcn-Vue CLI 新增 Admin Checkbox 与 Textarea 组件，生成源码保持原样。
- 新增两项 SSR 基础渲染测试；Admin 当前 19 个测试文件、47 项测试通过。
- 前端规范冻结基线从 4 个 Admin 文件 117 项收紧到 2 个 Admin 文件 60 项，新增违规为 0；WP1 总体仍在进行中。
- 同步更新下一步开发方案、阶段状态、模块地图、需求追踪矩阵、README 和 N2 第 11 次迭代记录。
- Windows PowerShell 5.1 统一门禁通过：API 92 项通过/1 个真实 Redis 条件测试跳过、覆盖率 90%，共享契约 2 项、Web 22 项、Admin 47 项、Desktop 11 项；迁移往返、前端 lint/typecheck/test/build、桌面 Release 构建、Compose 配置和 PowerShell 7 语法解析通过。浏览器 E2E、真实通知环境和生产放行仍为独立未关闭项。

### 完成 WP1 第三轮 Admin 登录与 TOTP 页面整改

- Admin 登录页迁移到 Button、Input、Label，保留用户名/邮箱、密码、可选动态验证码、忙碌态和错误反馈。
- Admin TOTP 设置页迁移到 Button、Input、Label，手工密钥改为只读组件，配置 URI 使用 Tailwind `break-all`，删除行内样式。
- 新增两项 SSR 基础渲染测试；Admin 当前 17 个测试文件、45 项测试通过。
- 前端规范冻结基线从 6 个文件 125 项收紧到 4 个 Admin 文件 117 项，新增违规为 0；WP1 总体仍在进行中。
- 同步更新下一步开发方案、阶段状态、模块地图、需求追踪矩阵、README 和 N2 第 10 次迭代记录。
- Windows PowerShell 5.1 统一门禁通过：API 92 项通过/1 项真实 Redis 条件测试跳过、共享契约 2 项、Web 22 项、Admin 45 项、Desktop 11 项通过；迁移往返、前端 lint/typecheck/test/build、桌面 Release 构建和 Compose 配置检查通过。

### 完成 WP1 第二轮用户案件与信誉页面整改

- 通过 Shadcn-Vue CLI 新增 Select 组件族，生成源码保持原样；Web 积分/信誉中心迁移到 Button、Progress 和 Tailwind 响应式布局。
- Web 举报/申诉案件中心迁移到 Button、Input、Label、Select、Textarea，并使用 Tailwind 语义颜色展示案件状态。
- 两张业务页面删除自定义 `<style>`、行内样式、硬编码十六进制颜色和原生表单控件，既有 API、幂等、权限与敏感信息边界保持不变。
- 新增两项 SSR 基础渲染测试；Web 当前 10 个测试文件、22 项测试通过。
- 前端规范冻结基线从 8 个文件 141 项收紧到 6 个 Admin 文件 125 项，新增违规为 0；WP1 总体仍在进行中。
- 同步更新下一步开发方案、阶段状态、模块地图、需求追踪矩阵、README 和 N2 第 9 次迭代记录。
- Windows PowerShell 5.1 统一门禁通过：API 92 项通过/1 项真实 Redis 条件测试跳过、共享契约 2 项、Web 22 项、Admin 43 项、Desktop 11 项通过；迁移往返、前端 lint/typecheck/test/build、桌面 Release 构建和 Compose 配置检查通过。

### 完成 WP1 第一轮前端质量门禁与用户基础流程整改

- 新增根/Web/Admin ESLint 配置和 `pnpm lint`，接入 `scripts/check.ps1` 与 GitHub Actions；统一脚本移除 PowerShell 7 专属语法，兼容 Windows PowerShell 5.1。
- 新增业务 Vue 规范策略与冻结基线，禁止新增自定义样式块、行内样式、硬编码十六进制颜色和原生表单控件，并排除 Shadcn-Vue CLI 生成目录的业务误报。
- 通过 Shadcn-Vue CLI 新增 Checkbox、Textarea、Progress；完成 App、首页、注册页的首批组件迁移，首页补充 SSR 基础渲染测试。
- 历史规范欠账由 11 个文件 164 项降至 8 个文件 141 项，新增违规为 0；WP1 总体仍在进行中，后续继续整改用户案件/信誉和 Admin 复杂页面。
- 同步更新下一步开发方案、阶段状态、模块地图、需求追踪矩阵、README 和 N2 第 8 次迭代记录。
- Windows PowerShell 5.1 统一门禁通过：Ruff、API 92 项通过/1 项真实 Redis 条件测试跳过、覆盖率 90%，共享契约 2 项、Web 20 项、Admin 43 项、Desktop 11 项通过；迁移往返、前端 lint/typecheck/test/build、桌面 Release 构建和 Compose 配置检查通过。


### 完成 WP0 Admin 角色审批工作台收口

- 新增 Admin `/role-changes` 导航、路由、服务和审批页面，接入角色变更请求筛选、分页、加载/空/错误态、详情、创建、批准和拒绝。
- 新增 `useRoleChanges` 状态控制器，处理加载竞态、分页边界、成功反馈保留、乐观并发错误和选中详情。
- 密码、TOTP 和一次性再认证令牌只在页面内存与单次请求体中短时存在；切换选中项、筛选、分页或敏感操作结束时清理。
- 补齐服务、状态控制器和 SSR 渲染测试，覆盖创建、批准、拒绝、撤销会话反馈、错误态、分页及凭据清理；既有后端测试继续覆盖双人复核、幂等重放、并发冲突和对象保护。
- 将角色变更四个 API 正式补入 v3.0 MVP API 表，并同步 API 约定、模块地图、阶段状态、需求追踪矩阵、执行方案、README 和 N2 第 6～7 次迭代记录。
- 统一本地门禁通过：API 92 项通过/1 项真实 Redis 条件测试跳过、覆盖率 90%，共享契约 2 项、Web 19 项、Admin 43 项、Desktop 11 项；Ruff、迁移往返、前端类型检查/测试/生产构建和桌面 Release 构建通过。

### 制定下一步开发方案并冻结 M5 准入路线

- 新增《密码侦探社下一步开发方案（2026-08-08）》，计划周期为 2026-08-10 至 2026-08-28，共 15 个工作日。
- 阶段目标冻结为全规格功能实现度不低于 95%、MVP 验收完成度不低于 88%、生产上线准备度不低于 60%，且 P0 阻塞缺陷清零。
- 执行路线分为 WP0 至 WP5：角色审批工作台收口、前端质量与规范门禁、账号申诉与案件编排、Playwright 核心旅程、安全/恢复/性能/可观测性准入、阶段复核与发布决策。
- N3 业务闭环包含账号申诉、案件分派与状态流转、处置结果通知、事务一致性、幂等、权限与审计；M5 准入包含 ESLint、Shadcn-Vue/Tailwind 整改、E2E、安全扫描、SBOM、备份恢复、性能基线和 Compose 目标环境证据。
- 本阶段不新增批量贡献、跨候选图谱、论坛、商城、移动端、新通知提供商或非必要大规模重构；这些事项继续保留在后续范围。
- 同步更新项目文档 README，将本方案设为当前执行基线，2026-08 计划调整为前序基线；本次仅修改 Markdown 文档，不修改产品代码。

## 2026-08-03

### 完成 N2 第 2 次审计查询与受限导出开发

- 规格和计划冻结 `GET /admin/audit-logs`、`GET /admin/audit-logs/{id}` 与 `GET /admin/audit-logs/export`，统一要求特权角色和当前 MFA 会话。
- 审计列表支持动作、结果、目标类型、操作者、请求号、时间范围和综合搜索；详情补充操作者用户名/角色，但不返回邮箱。
- 详情与 CSV 统一执行服务端递归脱敏、深度/数量/长度限制；CSV 增加 UTF-8 BOM 和公式注入防护。
- 同步导出限制为单次最多 5000 条、每分钟最多 10 次，并写入 `admin.audit.export`；列表和详情读取不写审计，避免自递归。
- Admin 静态占位页替换为使用 Shadcn-Vue 与 Tailwind 的真实审计工作台；同步共享契约、服务测试和基础渲染测试。
- 同步更新规格、下一阶段计划、API 约定、模块地图、阶段状态、需求追踪矩阵、README 和 N2 第 2 次迭代记录。
- 统一门禁通过：API 82 项通过/1 项真实 Redis 条件测试跳过、覆盖率 90%，共享契约 2 项、Web 19 项、Admin 24 项，桌面端 Release 构建与 11 项测试通过；迁移往返、Compose 配置和 `git diff --check` 通过。


### 完成 N2 第 1 次真实治理仪表盘开发

- 新增 MFA 门禁的 `GET /admin/dashboard/summary`，支持 1～720 小时窗口，聚合有效查询、命中、贡献、候选验证/隔离、已审计业务操作失败率和五类治理队列积压。
- 档案查询新增 `archive.search` 脱敏遥测；命中时只关联档案 ID，事件仅记录算法、命中/认证状态、请求号和脱敏 IP 网段，不保存完整指纹或候选秘密。
- Admin 静态仪表盘替换为真实指标卡、治理待办、生成时间和口径说明；同步共享 TypeScript 契约、服务层和测试。
- 明确当前错误率为已审计业务操作失败率，不等同于全量 HTTP 错误率；查询遥测从上线后累计，不回填演示数据。
- 同步更新规格、下一阶段计划、API 约定、模块地图、阶段状态、需求追踪矩阵、README 和 N2 第 1 次迭代记录。
- 统一全仓门禁通过：Ruff、API 80 项通过/1 项真实 Redis 条件测试跳过、覆盖率 90%，共享契约 2 项、Web 19 项、Admin 21 项通过；前端类型检查/生产构建、桌面端 Release 构建与测试、SQLite Alembic `base → head → base → head` 往返均通过；使用合成 `APP_SECRET_KEY` 的 `docker compose config --quiet` 与 `git diff --check` 通过。

### 完成 N1 第 4 次统一一次性再认证凭据开发

- 新增 `reauthentication_grants` 数据模型和 Alembic `20260803_0016`，凭据仅保存 SHA-256 摘要，默认 5 分钟过期并绑定用户、当前会话族和精确操作目的。
- 新增 `POST /me/security/reauthenticate`，要求当前密码，启用 TOTP 时还需动态码；响应不缓存，签发接口不使用幂等键，避免一次性秘密进入幂等响应记录。
- 密码修改、TOTP 停用和账号删除申请统一消费目的绑定的一次性凭据，消费与业务操作同事务，跨会话、跨目的、过期和重放均拒绝。
- 同步共享契约、Web 安全/隐私页面、API 约定、规格、下一阶段计划、阶段状态和需求追踪矩阵，并新增 API/Web 自动化测试。
- 统一门禁通过：API 78 项通过/1 项真实 Redis 条件测试跳过、覆盖率 90%，Web 19 项通过；Ruff、类型检查、前端构建、桌面构建/测试、迁移往返和 Compose 配置检查通过。
- 浏览器 Playwright、真实目标环境和管理员治理操作的细粒度再认证仍列为后续范围。

### 完成 N1 第 3 次认证恢复与 TOTP 登录开发

- 新增 Web 邮箱验证、忘记密码和密码重置页面，复用既有 API，并补齐 `/account/profile` 计划入口。
- 登录页新增可选 TOTP 验证码；账号资料页显示邮箱验证状态并支持重发验证邮件。
- 明确一次性邮箱验证和密码重置凭证只在页面内存及请求体中短时存在，读取后立即从地址栏移除，不写入浏览器持久化存储。
- 新增共享请求契约、公共认证服务和服务层自动化测试；同步 API 约定、阶段状态、需求追踪矩阵及本轮实施记录。
- 浏览器 Playwright、邮箱变更、统一短时再认证凭据、真实 SMTP/目标环境仍列为未关闭项。

### 完成 N1 第 2 次账号活动与隐私中心开发

- 新增揭示历史最小披露查询、版本化授权声明、个人数据导出和账号删除申请 API。
- 新增 `authorization_declarations`、`privacy_exports`、`privacy_deletion_requests` 数据模型及 `20260803_0015` 迁移。
- 明确导出内容不包含密码、候选秘密或 TOTP 密钥；下载凭证短时、一次性、所有权校验，并在消费/过期后清理制品。
- 明确账号删除需要当前密码再认证，TOTP 账号还需动态码，进入宽限期后可取消；到期 Worker 停用并去标识化账号，保留审计。
- 新增 Web `/account/activity` 与 `/account/privacy`，同步共享 TypeScript 契约、服务层测试和开发实施记录。
- 更新 API 约定、阶段状态和需求追踪矩阵；Docker Compose 默认使用 Celery 隐私任务后端并补充示例环境变量，配置解析门禁通过；真实 MySQL/Redis/Celery、浏览器 E2E、合规参数和生产验收仍列为未关闭项。

### 完成 N1 首批账号安全垂直切片

- 新增本人资料修改、密码修改和用户 TOTP 生成/确认/停用 API，并更新共享 TypeScript 契约与 API 约定。
- 明确用户名可以直接修改；邮箱变更必须使用后续独立验证流程，本轮不允许绕过邮箱验证。
- 明确密码修改要求当前密码近期再认证；已启用 TOTP 时还必须校验动态码；成功后保留当前会话并撤销其他会话。
- 明确 TOTP 生成响应中的明文密钥不进入幂等响应缓存、审计或日志；确认和停用写操作要求幂等键、限流和不可变审计。
- Web 用户安全页改造为资料、密码与 TOTP、登录会话三栏账号中心，并新增账号服务自动化测试。
- 新增《N1 第 1 次开发迭代：账号资料、密码与用户 TOTP》，阶段状态与需求追踪矩阵同步更新。

### 制定 M4 收口与 M5 准入的下一阶段开发计划

- 新增《密码侦探社下一阶段开发计划（2026-08）》作为当前执行计划，建议按单人 14 个工作日拆分为 G0 当前改动收口、N1 用户账号与隐私中心、N2 管理员治理中心、N3 案件编排与结果通知、N4 M5 准入门禁。
- 明确本阶段只关闭 P0 业务闭环并建立首批 P1 门禁，不新增通知渠道、跨候选图谱、自动处罚或其他非 MVP 功能。
- 为每个工作包补充模块范围、拟定 API、数据与安全约束、测试矩阵、出口条件、分支拆分、依赖关系和阶段级验收清单。
- 设定排程目标：全规格功能实现度 89%～91%、MVP 验收完成度 78%～82%、生产上线准备度 50%～55%；这些数值仅为计划目标，不作为放行结论。
- 项目文档索引、开发实施计划和阶段状态已链接到当前执行计划。

### 复核项目整体完成度并校正估算口径

- 新增《项目整体完成度审计（2026-08-03 复核）》：按 v3.0 全规格区分“已实现代码切片”和“完整 MVP 覆盖”，避免将 M4 已完成垂直切片误判为整个管理闭环完成。
- 复核 Web 用户端、Admin、Windows 桌面端、API/Worker、数据库迁移、Docker Compose、CI、GitHub PR 链和生产门禁。
- 记录当前估算：全规格功能实现度约 83%、MVP 验收约 69%、生产上线准备度约 45%；M4 调整为约 72%，明确用户中心、管理员治理、E2E、安全扫描、恢复、监控、合规和生产 SMTP 验收缺口。
- 记录 2026-08-03 本地门禁：API 67 项通过/1 项 Redis 条件测试跳过、覆盖率 91%；共享契约 2 项、Web 10 项、Admin 20 项、Desktop 11 项通过；类型检查、构建、迁移往返和 Docker Compose 空环境冒烟通过。
- 形成下一阶段 P0：用户中心账号/隐私闭环、管理仪表盘/审计/用户处置/配置、高风险二次确认、案件编排与结果通知；暂不继续扩展新的通知渠道。


### 初始化 Tailwind CSS 3 与 Shadcn-Vue 前端环境

- 在工作区根目录安装 Tailwind CSS 3、PostCSS、Autoprefixer 和 `tailwindcss-animate`，新增共享 Tailwind/PostCSS 配置。
- 为 Web 与 Admin 两个 Vue/Vite 应用配置 `@` 到各自 `src` 目录的路径别名，并同步 TypeScript 路径映射。
- 为两个应用完成 Shadcn-Vue 物理初始化，统一使用 New York 样式、Slate 基础颜色和 CSS Variables 颜色方案。
- 根目录及两个前端应用均新增 `components.json`；两个应用的 `src/index.css` 均接入 Tailwind 指令和 Slate 明暗主题变量。
- 保留当前 Shadcn-Vue CLI 作为 MCP 工具依赖；因当前 CLI 的样式枚举不再接受 New York/Slate，物理初始化使用兼容 Tailwind CSS 3 的 CLI 版本完成。
- 前端共享契约、Web、Admin 的类型检查、生产构建和测试全部通过。

### Web 简白科技感与可更换背景视觉改造

- 用户端 Web 统一调整为简白科技视觉语言，采用半透明白色表面、背景模糊、细描边、柔和投影和低饱和蓝紫交互色。
- 重构用户端顶部导航、品牌标识、按钮、表单、卡片、表格、状态提示和响应式布局；Admin 继续使用原有共享样式，不受本次用户端主题覆盖影响。
- 新增冰川白、数据网格、极光雾和晨曦环四套内置背景，选择结果保存在当前浏览器；支持上传不超过 10 MiB 的本地图片作为当前会话背景。
- 本地背景图片仅使用浏览器对象地址展示，不上传服务端、不写入项目数据，离开或刷新页面后自动释放。
- 完成 1440 px 首页视觉截图复核；Web 类型检查、10 项测试和生产构建全部通过。

## 2026-08-01

### 建立 v3.0 项目基线

- 阅读并整合项目文档目录内原有 8 份 Word 文档。
- 新建《密码侦探社项目规格说明书 v3.0》作为产品、需求、架构、安全和验收基线。
- 新建《密码侦探社开发实施计划 v1.0》，建立 14 周 MVP 实施路线。
- 统一“压缩包文件指纹 → 解压密码”的数据语义。
- 删除 `hash(password) == archive_file_hash` 的错误校验设计。
- 将桌面端可信模型由 User-Agent/硬件 ID 调整为安装密钥、挑战签名、防重放和风控证据。
- 统一候选状态为 `pending / verified / quarantined / rejected`。
- 将泄露口令爬虫、明文反查、论坛、商城、悬赏破解、AI 猜密等能力移出 MVP。

### 切换为 Markdown 唯一维护格式

- 将当前规格和开发计划移动到项目文档根目录，以 `.md` 作为唯一事实来源。
- 将原始 8 份 Word 文档转换为历史 Markdown。
- 将所有 DOCX 移动到 `历史文档/docx归档/`，不再维护。
- 新增《文档维护规范》，约定命名、版本、评审、归档、安全和变更流程。
- 更新项目文档索引和历史文档说明。

### 启动 M0/M1 分阶段模块化开发

- 创建单仓代码结构以及 API、Web、Admin、Windows Desktop、共享包、基础设施和开发文档目录。
- 新增 5 份 ADR、初始威胁模型、模块地图和需求测试追踪矩阵。
- 完成首个认证垂直切片、首批数据库迁移、会话轮换与重放检测。
- 完成 Vue 用户端/管理端骨架和 WPF 本地 SHA-256/MD5 计算 PoC。
- 新增 Docker Compose、CI 和统一质量检查脚本。
- M1 仍需完成 Redis 分布式限流、持久化幂等、邮箱验证/密码重置、TOTP 和容器环境实测。

## 2026-08-02

### 完成 M1 第二次认证与工程门禁迭代

- 初始化项目内独立 Git 仓库，并创建 M0/M1 基线提交。
- 新增 ADR-0006 与 ADR-0007，明确 Redis 分布式限流、数据库持久化幂等和浏览器会话边界。
- 扩展用户、会话、账号动作令牌和幂等记录数据模型，并新增对应 Alembic 迁移。
- 补充邮箱验证、密码重置、管理员 TOTP、MFA 声明和管理端访问门禁。
- 将 Web/Admin 刷新令牌迁移到独立、路径隔离的 HttpOnly Cookie，并保留桌面/API JSON 令牌流程。
- 更新 MVP API 表、API 约定、模块地图、测试追踪矩阵、本地开发指南和 M1 阶段状态。
- 新增真实 Redis CI 测试、迁移往返、镜像构建、Dependabot、Docker 空环境验收脚本和分支保护清单。
- M1 代码门禁完成；Docker 空环境、真实 Redis 托管 CI 和主分支保护仍需在具备外部环境后验收。

### 启动 M2 核心查询与贡献第 1 次迭代

- 新增档案、完整指纹、候选密码、贡献和积分流水数据模型及 Alembic 迁移。
- 实现候选密码 AES-GCM 密文保存、版本化密钥、独立 HMAC 去重标签和递归日志脱敏。
- 新增完整指纹精确查询、授权幂等贡献、重复候选合并、verified 候选揭示、每日配额、审计和个人贡献接口。
- 用户 Web 新增本地 SHA-256/MD5 分块计算、进度与取消、手工指纹输入、查询结果、贡献和个人贡献页面。
- 更新共享 API 契约、API 约定、模块地图、需求测试追踪矩阵、本地开发指南和阶段状态。
- API、Web、Admin、数据库迁移与 Windows Desktop 统一代码门禁通过；Docker Desktop 因 WSL 2/引擎未就绪仍不能执行空环境和真实 Redis 门禁。
- Git 远端 `origin` 已配置，开发分支已通过 Git 凭据推送；GitHub CLI 已安装但尚未登录，因此 PR、托管 CI 观察和分支保护仍待外部验收。

### 恢复 Docker Desktop 并补强本地容器门禁

- 修复 Docker Desktop 在 WSL 安装完成前启动后持续停留于失败状态的问题，并验证 Linux Engine、Compose 与最小容器运行正常。
- 为 Docker 空环境冒烟脚本增加非 ASCII 项目路径兼容：自动使用临时 ASCII 盘符执行 BuildKit 构建，结束后释放映射。
- 将门禁合成邮箱调整为 `synthetic.example.com` 示例域，避免新版邮箱校验器拒绝 `.invalid` 保留顶级域。
- Docker Compose 空环境门禁在约 0.5 分钟内通过；真实 Redis 双客户端分布式限流测试通过，M1 本地容器门禁关闭。

### 完成 M2 可信反馈、自动状态与积分结算第 2 次迭代

- 新增 ADR-0008，明确当前有效反馈、不可变证据历史、版本化规则、状态事件和首次验证积分结算边界。
- 新增候选反馈、验证证据历史和状态事件数据模型及 Alembic `20260802_0004`。
- 实现 `verification-v1`：两个独立成功且失败权重低于阈值时自动验证，三个独立失败或失败权重达到阈值时自动隔离。
- 新增幂等反馈和个人反馈历史 API；Web 查询页增加证据计数和成功/失败反馈入口。
- 候选首次进入 `verified` 时结算首位贡献者和有效成功验证者积分，首次验证前后的重复贡献均不重复奖励。
- 更新 API 约定、模块地图、阶段状态、本地开发指南、需求测试追踪矩阵和 `.env.example`。
- 新增专项自动化测试，并通过 Ruff、pytest 覆盖率、Alembic 往返、TypeScript、Vitest、Web/Admin 构建和 WPF Release 构建。
- 使用 Docker Compose 从空数据卷重建全栈，验证最新迁移、MySQL/Redis 健康检查、API/Worker/Web/Admin 启动与合成账号注册均通过。
- 创建 GitHub PR #14，并验证托管 CI 的 API、真实 Redis、迁移往返、前端、桌面端和容器镜像任务全部通过；主分支保护因私有仓库当前套餐限制暂不能配置。
- 桌面签名回执、防重放、复杂关联账号和管理端人工审核仍按 M3/M4 计划实施。
### 启动 M3 Windows 桌面验证第 1 次迭代

- 新增安装实例、一次性挑战和签名回执数据模型及 Alembic `20260802_0005`。
- 后端新增安装公钥注册/撤销、挑战获取、ECDSA P-256/SHA-256 回执验签、过期/重放/时钟/版本/绑定校验，并将有效桌面回执接入统一证据聚合器。
- Windows 客户端新增 ZIP/7z 本地目录枚举与受控内容读取、超时/取消及恶意压缩包资源限制。
- 首次启动生成随机安装 ID 与 P-256 密钥；私钥和桌面令牌均使用当前 Windows 用户 DPAPI 保护，不采集硬件标识。
- 桌面端完成登录、安装公钥注册、一次性挑战、规范载荷、ECDSA 签名与回执提交的首版闭环。
- 共享 API 契约新增桌面安装、挑战和回执类型；统一门禁新增桌面安全测试。
- 更新 API 约定、模块地图、阶段状态、测试追踪矩阵、本地开发指南和 M3 迭代记录。
- 使用 Docker Compose 从空数据卷验证 MySQL 最新迁移、Redis、API/Worker/Web/Admin 健康检查和合成账号注册，结束后完成资源清理。
- 创建堆叠 GitHub PR #15（基线为 M2 PR #14），并验证托管 CI 的 API、Frontend、Desktop 和 Container Images 任务全部通过。

### 完成 M3 桌面验证边界与恢复第 2 次迭代

- 新增可重建的合成 AES-256 ZIP 和加密头 7z 样本生成脚本、固定依赖和样本说明。
- 桌面验证资源限制改为集中配置，补充正确/错误密码、损坏/不支持格式、大小、条目、展开量、高压缩比、取消和路径安全测试矩阵。
- 安装身份服务新增 P-256 身份重建；客户端对撤销、账号绑定冲突、密钥不一致和安装缺失提供重新注册引导。
- 服务端最低客户端版本错误新增结构化版本详情，桌面端显示明确升级提示且不允许通过更换身份绕过。
- 更新 API 约定、模块地图、本地开发指南、阶段状态、需求测试追踪矩阵和 M3 第 2 次迭代记录。
- 桌面专项 10 个测试、M3 API 专项 6 个测试、统一质量门禁、Alembic 往返和 Docker 空环境冒烟通过；PR #15 本轮 GitHub CI 的 API、Frontend、Desktop 和 Container Images 全部通过。

### 完成 M3 后端桌面更新通道第 3 次迭代

- 新增 ADR-0009，明确后端托管发布清单、制品完整性、匿名下载和客户端显式升级入口的信任边界。
- 规格新增 DESK-09～12，并将“自动更新”拆分为本轮更新发现/显式下载与后续静默安装/正式签名流水线。
- 后端新增桌面发布模型、Alembic `20260802_0006`、管理员 MFA 创建/上传/发布/撤回接口和匿名检查/下载接口。
- 制品上传按声明大小与 SHA-256 流式校验并原子写入；发布前重新校验，撤回后停止下载，生产发布要求已验证签名元数据。
- Windows 桌面端启动后自动检查稳定通道，支持手动重试，并通过系统浏览器显式打开后端下载入口；不静默下载或执行。
- 更新 API 约定、模块地图、本地开发指南、阶段状态、需求测试追踪矩阵、实施计划和 M3 第 3 次迭代记录。
- 新增 API 更新发布专项测试与桌面更新协议测试；API 镜像依赖安装增加 BuildKit 缓存、超时和重试；前端镜像改为共享依赖阶段、复制锁文件并缓存 pnpm 仓库，以降低外部包索引瞬时抖动和重复安装对空环境门禁的影响。
- 本轮统一门禁与 Alembic 全量升降级往返已通过；Dockerfile/Compose 静态检查通过。本地 Docker 空环境重建在 2026-08-02 多次受外部 PyPI/npm 索引超时阻塞。
- PR #15 的 pull request CI 运行 `30737711202` 已通过，API、Frontend、Desktop 和 Container Images 全部成功，托管容器任务完成 API、Web、Admin 三个镜像构建。

### 完成 M3 管理端桌面发布工作台第 4 次迭代

- 规格新增 DESK-13，并将桌面发布工作台加入 MVP 管理端菜单与 M3 实施计划。
- 共享 API 契约新增桌面发布通道、架构、生命周期、签名状态、创建请求和发布记录类型。
- 管理端新增桌面发布页面，支持 MFA 会话下的列表、草稿创建、制品上传/重试、复核、发布和撤回。
- 管理端在提交前校验版本、最低版本关系、摘要格式、扩展名、文件大小和签名元数据；后端继续执行最终摘要与生命周期门禁。
- 二进制制品上传显式使用 `application/octet-stream`，JSON Content-Type 仅用于字符串请求体。
- 更新 ADR-0009、模块地图、本地开发指南、阶段状态、需求测试追踪矩阵、README 和 M3 第 4 次迭代记录。
- 新增 4 个管理端桌面发布服务测试和 1 个 API 内容类型边界测试，Admin 服务测试总数增至 7 个。
- 统一门禁、前端生产构建、Alembic 全量升降级往返、Docker Compose 配置校验和 Admin 前端镜像静态检查通过；PR #15 CI 运行 `30740786796` 的 API、Frontend、Desktop 和 Container Images 全部通过。

### 完成项目整体完成度审计并加固 MySQL 迁移门禁

- 新增 `docs/development/2026-08-02-completion-audit.md`，按规格、计划、代码、测试、环境和 Git 交付状态评估 M0～M5。
- 工程估算为功能代码约 75%、MVP 验收约 60%、生产上线准备度约 40%；百分比仅用于规划，不替代目标环境验收或产品签字。
- 更新阶段状态，补齐 M4/M5 明确状态、9 份 ADR、M3 第 4 次迭代链接和未关闭的 E2E、实机、性能、KMS、签名、合规与运维门禁。
- Docker MySQL 8.4 空环境发现 Alembic `20260802_0006` 的 `TEXT` 空字符串默认值不兼容，移除服务端默认值后全量迁移、健康检查和合成注册/登录在约 0.9 分钟内通过。
- GitHub Actions API 作业增加 MySQL 8.4 服务与 Alembic 全量升降级往返，同时保留 SQLite 迁移门禁，避免数据库方言问题再次仅在运行时暴露。
- 首轮托管 MySQL 往返运行 `30745274582` 进一步发现 InnoDB 不允许先删除外键所需索引；`20260802_0006` 降级改为直接删除整表，由数据库同时移除索引和外键，修复后本地 MySQL 8.4 全量升降级往返通过。
- PR #15 CI 运行 `30745600742` 的 API、Frontend、Desktop 和 Container Images 全部通过；API 作业中的 SQLite 与 MySQL 8.4 全量升降级往返均成功。
- 更新 README 和本地开发指南，明确当前阶段、完成度口径以及 SQLite 本地门禁与 MySQL 托管门禁的分工。

### 完成 M4 候选人工审核闭环第 1 次迭代

- 规格细化管理端候选筛选、最小披露详情、`moderation-v1` 人工状态矩阵、受控原因码、MFA/幂等/审计约束和人工状态事件字段。
- 数据模型扩展 `record_state_events`，增加状态来源、操作者、请求号和可选原因说明；新增 Alembic `20260802_0007`。
- 后端新增 `modules/moderation`，实现 `GET /admin/candidates`、`GET /admin/candidates/{id}` 和 `POST /admin/candidates/{id}/transition`。
- Admin 新增候选审核队列、指纹/证据/状态时间线详情和隔离、恢复、通过、拒绝入口；共享契约增加对应类型。
- 更新实施计划、API 约定、模块地图、阶段状态、需求测试追踪矩阵和 M4 第 1 次迭代记录。
- 新增 3 个 API 审核集成测试和 3 个 Admin 候选审核服务测试；API 当前 43 通过、1 个真实 Redis 条件测试跳过，覆盖率 89.66%；Admin 当前 10 项测试通过。
- 统一门禁通过：SQLite 全量迁移往返、全部工作区类型检查/测试/构建、Windows Desktop Release 构建及 11 项测试均成功。
- MySQL 8.4 回滚验证发现 `20260802_0007` 不能先删除外键依赖索引，调整为先删除外键约束后，MySQL 全量升级、回滚、再升级通过。
- Docker Compose 空环境构建、MySQL/Redis/API/Worker/Web/Admin 健康检查、合成注册/登录和前端可用性冒烟通过，耗时约 10.6 分钟。
- 创建堆叠 PR #16（基于 PR #15 分支）；GitHub Actions 运行 `30749220990` 的 API、Frontend、Desktop 和 Container Images 全部通过，其中 API 同时通过 SQLite 与 MySQL 8.4 全量迁移往返。
### 完成 M4 举报与申诉案件闭环第 2 次迭代

- 新增 ADR-0010，确定举报与贡献审核申诉共用 `trust_cases`/`trust_case_events`，并保持案件结论与 `moderation-v1` 候选状态转换的独立审计边界。
- 规格细化案件类型、状态矩阵、贡献者申诉授权、对象级私有列表、MFA 管理队列、受控结果码、幂等、限流和审计脱敏规则。
- 新增 Alembic `20260802_0008` 和 `modules/trust_cases`，实现举报、申诉、本人案件列表、管理筛选/详情/状态转换和不可变事件。
- 用户 Web 新增举报/申诉案件中心，查询结果提供举报快捷入口，被拒绝或隔离的本人贡献提供申诉快捷入口。
- Admin 新增统一举报/申诉队列、详情、处理结果和事件时间线；共享 API 契约增加创建、列表、事件和转换类型。
- 更新实施计划、API 约定、模块地图、阶段状态、需求测试追踪矩阵、README 和 M4 第 2 次迭代记录。
- 新增 3 个 API 案件集成测试、3 个 Web 服务测试和 3 个 Admin 服务测试；API 46 通过、1 个真实 Redis 条件测试跳过，覆盖率 84%；Web 9 项、Admin 13 项、Desktop 11 项测试通过。
- 统一门禁、SQLite/MySQL 8.4 全量迁移往返和 Docker Compose 空环境烟测通过；容器烟测完成 MySQL/Redis/API/Worker/Web/Admin 健康检查和合成注册/登录，耗时约 11.4 分钟。
- 创建堆叠 PR #17（基于 PR #16 分支）；GitHub Actions 运行 `30752802023` 的 API、Frontend、Desktop 和 Container Images 全部通过，其中 API 同时通过 SQLite 与 MySQL 8.4 全量迁移往返。

### 完成 M4 积分与信誉中心第 3 次迭代

- 新增 ADR-0011，明确积分账本投影、初始 50 分、0～100 信誉边界和不可变信誉事件。
- 新增 `reputation_events` 与 Alembic `20260802_0009`，同步调整规格中的 M4 第 3 个垂直切片规则。
- 首次验证结算新增首位贡献者和有效成功验证者信誉奖励，引用唯一约束和用户行锁避免重复加分。
- 达到 0/100 信誉边界时仍追加实际变化量为 0 的事件以占用引用幂等键，避免分值变化后重放旧奖励。
- 新增本人积分汇总、积分流水、信誉事件 API，以及 Web 积分/信誉/反馈历史中心。
- 更新开发计划、阶段状态、模块地图、API 约定、需求测试追踪矩阵和项目索引。
- 统一门禁通过：API 49 项通过、1 项真实 Redis 条件测试跳过、覆盖率 90%；共享契约 2 项、Web 10 项、Admin 13 项、Desktop 11 项测试通过；前端类型检查/生产构建及 SQLite/MySQL 8.4 全量迁移往返通过。
- Markdown 相对链接检查覆盖 51 个文件，变更文件敏感信息模式扫描通过。
- 创建堆叠 PR #18（基于 PR #17 分支）；GitHub Actions PR 运行 `30758736599` 与 push 运行 `30758710904` 的 API、Frontend、Desktop 和 Container Images 作业全部通过。
- 候选后续撤销的补偿/扣减、管理员用户处置和异常关联告警继续保留为后续范围。

### 完成 M4 奖励补偿与信誉校正第 4 次迭代

- 新增 ADR-0012，明确 `reward-compensation-v1` 的状态驱动资格重算、不可变追加、来源边界和同事务原则。
- 新增 `reward_adjustment_events` 数据模型与 Alembic `20260802_0010`，同步更新规格核心表和管理 API 定义。
- 自动与人工候选状态转换均接入积分补偿和信誉校正，人工首次验证补齐原始奖励结算；重复流转保持幂等，原始奖励事件保持不可变。
- 管理端新增奖励校正汇总和不可变时间线，用户积分/信誉中心新增奖励失效与恢复标签，审计保持聚合和最小披露。
- 新增多轮撤销/恢复、自动/人工流转、首次人工验证、幂等和最小披露测试；统一门禁及 SQLite/MySQL 8.4.11 迁移往返通过。
- 创建堆叠 PR #19；GitHub Actions PR 运行 `30763022872` 与 push 运行 `30763006406` 的 API、Frontend、Desktop 和 Container Images 作业全部通过。

### 完成 M4 失败激增风险告警第 5 次迭代

- 新增 ADR-0013，明确 `risk-alert-v1` 的 15 分钟窗口、独立失败阈值、同事务创建、活跃告警去重和聚合隐私边界。
- 新增 `risk_alerts`、`risk_alert_events` 与 Alembic `20260802_0011`，同步更新规格核心表和管理 API 定义。
- 验证证据接入失败激增检测；达到阈值时继续自动隔离候选，并创建只包含窗口、独立失败数和权重的高危告警。
- 新增 MFA 风险告警队列、详情、确认/解决/重开状态机、持久化幂等、不可变事件、限流与最小披露审计。
- 管理端新增风险告警导航、仪表盘入口、聚合证据详情、受控处置和不可变时间线；共享 TypeScript 契约同步更新。
- 新增后端与管理端专项测试；统一门禁通过：API 54 项通过、1 项真实 Redis 条件测试跳过，共享契约 2 项、Web 10 项、Admin 16 项、Desktop 11 项通过，SQLite/MySQL 8.4.11 迁移往返通过。
- 当前估算更新为功能代码约 88%、MVP 验收约 73%、生产上线准备度约 44%、M4 约 84%；关联账号图谱、动态降权、告警通知/SLA 和自动处罚仍为后续范围。
- 创建堆叠 PR #20；GitHub Actions push 运行 `30765953749` 与 PR 运行 `30766125682` 的 API、Frontend、Desktop 和 Container Images 作业全部通过。


### 完成 M4 关联证据与动态降权第 6 次迭代

- 新增 ADR-0014，冻结 `correlation-v1` 候选内关联图、传递关联组、成功/失败维度动态限权和最小披露边界。
- 新增 `evidence_correlation_assessments` 与 Alembic `20260803_0012`；每次有效反馈材料变化在同一事务内追加不可变聚合快照。
- `verification-v2` 接入关联分析后的有效权重；候选详情增加脱敏关联组、账号/反馈标识、信号类型及原始/有效权重历史。
- 更新项目规格、开发实施计划、API 约定、模块地图、阶段状态、需求追踪、README 和 ADR 索引；明确跨候选图谱、通知/SLA 与自动处罚仍未实现。
- 新增候选关联单元/集成测试；统一门禁通过：API 57 项通过、1 项真实 Redis 条件测试跳过、覆盖率 90.99%，共享契约 2 项、Web 10 项、Admin 16 项、Desktop 11 项通过。
- SQLite/MySQL 8.4.11 全量迁移往返、真实 Redis 8 项安全门禁和 Docker Compose 空环境冒烟通过；合成注册/登录耗时约 3.2 分钟，资源已清理。
- 创建堆叠 PR #21；GitHub Actions push 运行 `30775811539` 与 PR 运行 `30775831810` 的 API、Frontend、Desktop 和 Container Images 作业全部通过。

### 完成 M4 告警 SLA、值班指派与异步通知第 7 次迭代

- 新增 ADR-0015，确定 `risk-alert-sla-v1` 的 15 分钟响应、240 分钟解决目标，以及历史告警保存规则版本和绝对截止时间的原则。
- 扩展 `risk_alerts` 和 `risk_alert_events`，新增 `risk_alert_notifications` 事务 Outbox 与 Alembic `20260803_0013`。
- 新增可用 MFA 操作者列表、负责人/超时筛选和幂等指派 API；负责人变化进入不可变时间线和最小披露审计。
- Celery Worker 新增 SLA 扫描与 Outbox 投递任务，通知按事件/接收人去重、最多尝试 3 次且不复制邮箱、IP、安装哈希或关联键。
- Admin 风险工作台新增 SLA 徽标、响应/解决截止时间、负责人指派、超时筛选和通知历史；共享 API 契约同步扩展。
- 更新规格、实施计划、API 约定、模块地图、阶段状态、需求测试追踪矩阵、README 和 M4 第 7 次迭代记录。
- 统一门禁通过：API 默认环境 59 项通过/1 项真实 Redis 条件测试跳过，真实 Redis 环境 60 项全部通过且覆盖率 90.67%；共享契约 2 项、Web 10 项、Admin 18 项、Desktop 11 项通过。
- SQLite/MySQL 8.4 全量迁移往返和 Docker Compose 空环境冒烟通过；合成注册/登录耗时约 1.0 分钟，资源已清理。
- 创建堆叠 PR #22；GitHub Actions push 运行 `30778810391` 与 PR 运行 `30778846715` 的 API、Frontend、Desktop 和 Container Images 作业全部通过。

### 完成 M4 通知投递运维与死信重放第 8 次迭代

- 新增 ADR-0016，冻结 `notification-webhook-v1` 的 HTTPS、规范 JSON、UTC 时间戳、HMAC-SHA256 签名、提供商回执和最小披露边界。
- 新增 Alembic `20260803_0014`，为 `risk_alert_notifications` 增加提供商、回执、失败时间和人工重放审计元数据；第三次失败进入终态。
- 新增统一通知网关工厂和 `memory/log/webhook` 后端配置；API 与 Worker 共用同一选择逻辑，Webhook URL 仅允许 HTTPS，签名密钥至少 32 字符。
- 新增风险通知投递指标、分页过滤和幂等人工重放 API；重放只接受失败记录，要求 MFA、原因、限流和 `Idempotency-Key`，并写入最小披露审计。
- Admin 风险工作台新增投递指标卡、失败通知队列、告警跳转和手动重放入口；详情展示提供商、回执、失败时间和重放次数，不展示邮箱、Webhook 地址、密钥或原始载荷。
- 同步更新 v3.0 规格、开发实施计划、API 约定、模块地图、阶段状态、需求测试追踪矩阵、本地开发指南、README、ADR 索引和 M4 第 8 次迭代记录。
- 统一本地门禁通过：Ruff、API 默认环境 62 项通过/1 项真实 Redis 条件测试跳过、覆盖率 90.76%，真实 Redis 环境 63 项全部通过；共享契约 2 项、Web 10 项、Admin 20 项、Desktop 11 项通过，前端类型检查/构建和桌面 Release 构建通过。
- SQLite/MySQL 8.4 全量迁移 `upgrade → downgrade → upgrade`、`docker compose config` 和 Docker Compose 空环境冒烟通过；合成注册/登录耗时约 1.2 分钟，资源已清理。
- 创建堆叠 PR #23；GitHub Actions push 运行 `30784336062` 与 pull_request 运行 `30784366021` 的 API、Frontend、Desktop 和 Container Images 作业全部通过。

### 完成 M4 SMTP 邮件投递第 9 次迭代

- 用户确认邮件发信使用 SMTP；新增 ADR-0017，冻结提供商无关 SMTP、STARTTLS/SSL、认证、秘密管理、纯文本模板和 Message-ID 语义。
- 统一通知网关新增 `smtp` 后端，覆盖账号邮箱验证、密码重置和风险告警；风险告警复用原 Outbox、有限重试、失败终态、指标和人工重放，无需新增数据库迁移或 HTTP API。
- 新增 SMTP 主机、端口、安全模式、用户名、密码、发件地址、发件人名称和超时配置；非本地环境强制认证和 TLS，密码使用秘密类型脱敏。
- `.env.example` 与 API/Worker Compose 服务同步增加 SMTP 合成占位配置；文档不记录真实服务商账号、密码、令牌或个人邮箱。
- 新增 `apps/api/tests/test_notification_smtp.py`，覆盖 STARTTLS、隐式 SSL、认证、纯文本模板、最小披露、Message-ID、收件人拒绝和生产配置门禁。
- 同步更新 v3.0 规格、开发实施计划、模块地图、阶段状态、需求追踪矩阵、本地开发指南、README、ADR 索引和 M4 第 9 次迭代记录。
- 统一本地门禁通过：SMTP/Webhook 专项测试 7 项，API 默认环境 67 项通过/1 项真实 Redis 条件测试跳过、覆盖率 91%，真实 Redis 分布式限流测试 1 项通过；共享契约 2 项、Web 10 项、Admin 20 项、Desktop 11 项通过。
- Ruff、前端类型检查/生产构建、桌面 Release 构建、SQLite/MySQL 8.4 全量迁移往返、`docker compose config` 和 Docker Compose 空环境冒烟通过；最终复验合成注册/登录耗时约 7.7 分钟，资源已清理。创建堆叠 PR #24；GitHub Actions push 运行 `30786947956` 与 pull_request 运行 `30786976922` 的 API、Frontend、Desktop 和 Container Images 作业全部通过。

### 2026-08-03：N2 第 3 次迭代——用户治理只读总览

- 新增管理员专属 `GET /admin/users` 和 `GET /admin/users/{user_id}`，要求 `admin` 角色与当前会话 MFA；支持状态、角色、用户 ID/用户名/邮箱筛选和分页。
- 新增最小披露用户治理响应：邮箱掩码、TOTP 状态、活跃/历史会话、积分余额、信誉事件、提交、举报/申诉和隐私请求队列统计。
- 新增 Admin 用户治理工作台、详情侧栏、共享契约和 API/Admin 页面测试；本轮保持只读，封禁、解封和角色变更等待管理员近期再认证门禁。
- 同步更新 v3.0 规格、API 约定、模块地图、阶段状态、下一阶段计划、README 和本轮开发记录。

## 2026-08-03：N2 第 4 次迭代——管理员危险操作安全链

- 更新 `密码侦探社项目规格说明书-v3.0.md`：增加管理员专用再认证、用户停用/恢复和会话撤销接口及安全约束。
- 更新 `密码侦探社下一阶段开发计划-2026-08.md`：记录 N2 第 4 次迭代进展，并将下一入口调整为配置版本治理。
- 更新 API 约定、模块地图、阶段状态、需求追踪矩阵和 README；新增 `docs/development/2026-08-03-n2-iteration-4.md`。
- 冻结首批管理员危险操作门禁：管理员 + MFA、当前密码、TOTP、一次性目的凭据、结构化原因、持久化幂等、乐观并发、对象级保护和不可变审计。
- 明确角色变更、管理员/服务账号处置和批量处置继续关闭，等待权限层级、双人复核与资源上限设计。



## 2026-08-03（N2 第 4 次迭代门禁结果回填）

- 更新 `docs/development/2026-08-03-n2-iteration-4.md`：回填统一本地门禁统计，记录 API 89 项测试中 88 项通过、1 项真实 Redis 条件测试跳过、覆盖率 90.46%，以及共享契约、Web、Admin、桌面端测试结果。
- 更新 `docs/development/phase-status.md`：将 N2 第 4 次迭代标记为统一本地门禁完成，细化用户处置闭环和安全验收剩余范围。


## 2026-08-04：N2 第 5 次迭代——系统配置版本治理

- 更新 `密码侦探社项目规格说明书-v3.0.md`：新增配置版本列表、详情、草稿、发布和回滚接口及固定 Schema、安全门禁和运行时投影约束。
- 更新 `密码侦探社下一阶段开发计划-2026-08.md`：回填 N2 第 5 次迭代结果，并将下一入口调整为角色层级与双人复核模型。
- 更新 API 约定、模块地图、阶段状态、需求追踪矩阵和 README；新增 `docs/development/2026-08-04-n2-iteration-5.md`。
- 明确回滚创建新的不可变发布版本，不改写历史；发布与回滚要求配置目的的一次性再认证、原因码、幂等、乐观并发和最小披露审计。
- 首批动态配置只包含每日揭示配额、再认证 TTL、隐私删除宽限期、桌面最低版本和升级下载缓存；文档与示例不记录真实密码、令牌、密钥或个人信息。

## 2026-08-04：N2 第 6 次迭代——受控角色变更双人复核后端闭环

- 更新 `密码侦探社项目规格说明书-v3.0.md`：冻结普通角色转换矩阵、双管理员复核、再认证、并发、幂等、会话撤销和审计约束。
- 更新 `密码侦探社下一阶段开发计划-2026-08.md`、API 约定、模块地图、阶段状态和需求追踪矩阵；新增 `docs/development/2026-08-04-n2-iteration-6.md`。
- 新增 `role_change_requests` 表和 `20260804_0018_n2_role_change_requests.py` 迁移；接入角色变更请求列表、创建、批准和拒绝 API。
- 新增后端角色治理专项测试，覆盖双人复核、自审拒绝、角色/状态乐观并发、会话撤销、幂等、令牌消费顺序和最小披露审计。
- 统一本地门禁通过：API 91 项通过、1 项真实 Redis 条件测试跳过；共享契约 2 项、Web 19 项、Admin 33 项、桌面端 11 项测试，以及 Ruff、类型检查、生产构建、迁移往返、Compose 配置和差异检查通过。
- Admin 角色变更审批工作台、独立事件时间线、紧急撤权、批量处置和管理员/服务账号治理继续列为后续范围。

## 2026-08-08

### WP2 第 2 次迭代：案件指派、重开与乐观并发

- 更新 v3.0 规格与 API 约定：冻结 `expected_version`、`trust_cases.version`、独立 `assign`/`reopen` 路径、启用审核员/管理员指派约束、版本冲突错误和负责人前后事件快照。
- 更新下一步开发方案、模块地图、阶段状态、需求追踪矩阵和 README；明确原子 `resolve`、关联副作用、奖励/信誉补偿、通知 Outbox、SLA 与 Web 账号申诉交互仍待后续轮次。
- 对应代码：`20260808_0020_wp2_case_assignment_reopen.py`、trust case 模型/服务/路由/契约、Admin 案件页和服务测试。
- 验证范围：案件指派/重开幂等重放、过期版本冲突、负责人前后事件快照、非启用指派对象拒绝、API/Admin/共享契约测试。统一门禁实测通过：API 97 项（96 通过、1 项真实 Redis 条件测试跳过）、覆盖率 93.07%；共享契约 2 项、Web 23 项、Admin 50 项、Desktop 11 项测试通过；Ruff、前端规范检查、类型检查、生产构建、桌面 Release 构建及 SQLite 全量迁移往返通过。

## 2026-08-09：WP3 第 2 次迭代——Web 查询、揭示、贡献与举报核心旅程

- 新增 `docs/development/2026-08-09-wp3-iteration-2.md`，记录本轮范围、数据安全约束、本地验证和未关闭项。
- 更新阶段状态、下一步开发方案和需求追踪矩阵：补齐 Web 已验证查询/揭示/清除、授权贡献、候选举报和本人案件时间线的浏览器证据。
- 更新 Playwright 合成配置与隔离 API 种子，使用合成 Web 账号、指纹和加密候选字段；新增 `tests/e2e/web-core-journeys.spec.ts`，不记录真实密码、令牌或个人信息。
- 统一本地门禁 `.\scripts\check.ps1 -SkipInstall -IncludeE2E` 通过，包含新增后的 7 项 Playwright Chromium 旅程。
- 远端 GitHub Actions `31292352350` 的 API、Frontend、Desktop、E2E 和 Container Images 5 个作业全部通过。
- 本轮下一入口调整为 Admin 候选/案件处置，之后继续关闭 Web 账号安全、视觉、移动端、可访问性和目标环境验收。
## 2026-08-09：WP3 第 3 次迭代——Admin 候选审核与案件原子处置

- 新增 `docs/development/2026-08-09-wp3-iteration-3.md`，记录 Admin 候选人工审核、举报案件指派/处理/解决、关联候选原子隔离和操作反馈修复。
- 更新阶段状态、下一步开发方案和需求追踪矩阵：补齐两条 Admin Chromium 核心旅程的实现与自动化证据，并将下一入口调整为 Web 账号安全及更多 Admin 治理旅程。
- 扩展 Playwright 合成配置和隔离 API 种子，增加独立 MFA 工作流管理员、待验证候选、举报候选和待处理举报案件；不记录真实密码、令牌、密钥或个人信息。
- 修复 Admin 案件写操作成功提示在详情刷新时被立即清空的问题；定向 Admin E2E 2 项和 Admin 51 项测试通过。
- 统一本地门禁 `.\scripts\check.ps1 -SkipInstall -IncludeE2E` 通过，包含 9 项 Playwright Chromium 旅程；远端 GitHub Actions `31294494635` 的 API、Frontend、Desktop、E2E 和 Container Images 5 个作业全部通过。

## 2026-08-09：WP3 第 4 次迭代——Web 用户安全与隐私浏览器闭环

- 新增 `docs/development/2026-08-09-wp3-iteration-4.md`，记录密码/会话、TOTP 和隐私中心三条 Web 浏览器闭环、敏感数据约束与未关闭项。
- 更新阶段状态、下一步开发方案和需求追踪矩阵：将 Web 密码修改、会话撤销、TOTP 启停/登录门禁、隐私用途确认、一次性导出和账号删除请求创建/撤销标记为本地 Chromium 闭环。
- 扩展 Playwright 合成配置和隔离 API 种子，增加三个独立 Web 用户与两个不可用于登录的合成刷新哈希；保持生产 `auth.login` 速率限制不变，不记录真实密码、令牌、密钥或个人信息。
- 定向 Web E2E 3 项、Admin 回归 E2E 2 项和完整 12 项 Playwright Chromium 旅程通过；ESLint、E2E TypeScript 与种子 Ruff 检查通过。
- 统一本地门禁 `./scripts/check.ps1 -SkipInstall -IncludeE2E` 通过，覆盖 API、迁移往返、前端、桌面端和 12 项 Chromium 旅程；远端 GitHub Actions `31296867172` 的 API、Frontend、Desktop、E2E 和 Container Images 五个作业全部通过，下一轮进入 Admin 用户治理、角色审批、配置和风险告警浏览器闭环。

## 2026-08-09：WP3 第 5 次迭代——Admin 用户治理与角色双人审批闭环

- 新增 `docs/development/2026-08-09-wp3-iteration-5.md` 和 `tests/e2e/admin-governance-journeys.spec.ts`。
- 扩展隔离合成种子，覆盖普通用户停用/恢复、活跃会话撤销，以及不同管理员角色变更申请/批准和目标会话撤销。
- 定向 Admin E2E 2 项与包含 14 项 Chromium 旅程的统一本地门禁通过；远端 GitHub Actions `31298489273` 的 API、Frontend、Desktop、E2E 和 Container Images 五个作业全部通过。
- 下一轮继续系统配置和风险告警浏览器闭环。

## 2026-08-09：WP3 第 6 次迭代——系统配置与风险告警浏览器闭环

- 新增 `docs/development/2026-08-09-wp3-iteration-6.md` 和 `tests/e2e/admin-operations-journeys.spec.ts`。
- 扩展隔离合成种子，覆盖高风险候选、失败证据和待响应风险告警；增加配置草稿/发布/历史回滚与告警指派/核查/解决两条 Chromium 旅程。
- 修复系统配置和风险告警页面在写操作后刷新详情时清空成功提示的问题。
- Admin ESLint、TypeScript 和 51 项测试、定向 Admin E2E 2 项、完整 16 项 Chromium 旅程及统一本地门禁通过；远端 GitHub Actions `31299937196` 的 API、Frontend、Desktop、E2E 和 Container Images 五个作业全部通过。
- 下一轮继续 Web 邮箱/刷新异常组合、视觉快照、移动端和可访问性门禁。

## 2026-08-09：WP3 第 7 次迭代——Web 邮箱、刷新令牌与移动端键盘门禁

- 新增 `docs/development/2026-08-09-wp3-iteration-7.md` 和 `tests/e2e/web-identity-journeys.spec.ts`。
- 扩展隔离合成种子，覆盖未验证用户、一次性邮箱验证凭证和两组可轮换刷新会话；数据库只保存凭证摘要，旅程关闭截图与 Trace。
- 新增邮箱待验证/一次性验证/重放拒绝、刷新令牌轮换/重放/令牌族撤销，以及 390 × 844 移动视口无横向溢出和 Enter 键返回登录三条 Chromium 门禁。
- 保持生产 `web.auth.login` 每分钟 10 次限流不变，改用预置刷新会话建立隔离浏览器状态，避免全量套件共享来源地址导致伪失败。
- 定向 Web 身份 E2E 3 项、完整 19 项 Chromium 旅程及 `scripts/check.ps1 -SkipInstall -IncludeE2E` 统一门禁通过；远端 GitHub Actions `31301934873` 的 API、Frontend、Desktop、E2E 和 Container Images 五个作业全部通过。
- 修复 Admin 候选审核浏览器用例在 CI 重试时重复处置已完成候选的问题；同一 Playwright 服务与数据库连续执行 2 次通过，远端稳定性运行 `31302927800` 五个作业全部通过。

## 2026-08-09：WP3 第 9 轮已登录核心页面无障碍与响应式收口

- 新增 `docs/development/2026-08-09-wp3-iteration-9.md`，记录 Web/Admin 主内容跳转、移动/桌面视觉基线、反馈实时语义、Select 可辨识名称及可重复合成会话。
- 更新阶段状态、下一步开发方案和需求追踪矩阵：将已登录 Web 账号安全/隐私、Admin 用户治理/角色审批的键盘跳转、响应式边界和 Axe 严重/关键门禁记为本地完成；完整 Chromium 旅程更新为 28 项。
- 更新 Web/Admin 应用壳与核心页面语义；补充独立合成刷新会话和 Admin MFA 已验证种子，不改变生产登录限流或真实凭据处理。
- 统一本地门禁 `./scripts/check.ps1 -SkipInstall -IncludeE2E` 通过；远端 GitHub Actions `31307983187` 的 API、Frontend、Desktop、E2E 和 Container Images 五个作业全部通过。


## 2026-08-10：WP4 第 7 次迭代——性能基线与可观测性出口

- 新增 `GET /api/v1/metrics`、低基数 HTTP 直方图、Database/Redis/Worker 可用性、Celery 队列积压和进程运行时长指标。
- 新增请求关联上下文、结构化 HTTP 完成日志、Worker Redis 心跳及敏感字段双层脱敏。
- 新增隔离性能脚本、机器证据校验器、专项测试、统一门禁 `-IncludePerformance` 和 CI `performance` 作业。
- 本地实测 liveness 99.808 QPS/P95 37.308 ms，精确查询 10.043 QPS/P95 96.998 ms，两项错误率均为 0%。
- 更新规格 API 表、API 约定、阶段状态、需求追踪、下一步开发方案、README、本地开发说明和运行手册；下一轮进入告警规则、Grafana 仪表盘和 Worker 队列积压演练。

## 2026-08-10：WP4 第 8 次迭代——告警、仪表盘与 Worker 队列积压演练

- 新增 Prometheus 抓取配置和 7 条低基数告警规则，统一引用 `docs/runbooks/wp4-monitoring.md`，并禁止请求 ID、用户 ID、邮箱、指纹、令牌和密钥等敏感/高基数字段进入规则标签。
- 新增 Grafana provisioning、固定 datasource UID 和 6 面板总览仪表盘；新增监控 Compose overlay，支持本地隔离 smoke。
- 新增监控静态校验器、Prometheus `promtool` 校验、Worker 队列积压演练及证据 SHA-256 校验；24 个合成任务已验证队列从 24 降至 0。
- 将监控配置和队列演练纳入 `scripts/check.ps1 -IncludeMonitoring` 与 GitHub Actions `monitoring` 作业。
- 同步更新阶段状态、下一步开发方案、可观测性说明和 WP4 第 8 次迭代记录；生产 Alertmanager 通知闭环、值班恢复、多 Worker 长时混合压测和目标环境验收仍未完成。

## 2026-08-10：WP4 第 9 次迭代——Alertmanager 通知闭环与安全演练

- 新增 Alertmanager 配置、Prometheus 转发、低基数分组、`send_resolved` 和隔离通知网关。
- 新增通知网关健康检查、合成通知接收、敏感字段 `[REDACTED]` 脱敏和事件查询；不保存原始请求正文或请求头。
- 新增 firing/resolved 通知演练与证据校验：相同 firing 去重一次、resolved 交付一次、delivery ID 唯一、原始合成秘密标记不进入报告。
- 将 `amtool check-config`、Alertmanager 通知演练、证据 SHA-256 与既有 Worker 积压演练纳入 `check.ps1 -IncludeMonitoring` 和 GitHub Actions `monitoring` 作业。
- 新增 WP4 第 9 次开发记录、Alertmanager 运行手册，并同步阶段状态、下一步开发方案和可观测性说明。正式值班渠道、TLS/认证、回执升级、多实例 HA、密钥轮换和目标环境验收仍未完成。

## 2026-08-10（WP4 第 9 次迭代 CI 健康检查收口）

- 根据首次远端 Alertmanager 演练证据，将通知网关与 Alertmanager 容器健康检查地址由 `localhost` 固定为 `127.0.0.1`。
- 关闭 Alpine BusyBox `wget` 优先访问 IPv6、而合成通知网关仅监听 IPv4 导致的伪不健康问题；通知功能和验收标准不变。
- 同步补充 `docs/development/2026-08-10-wp4-iteration-9.md` 的 CI 复核记录。


## 2026-08-10：WP4 第 14 次迭代——目标环境采集与 HA 事件适配器

- 新增资源采样与 MySQL/Redis HA 事件源合同，要求统一执行组、UTC 时钟偏差、严格采样覆盖、Worker 故障恢复和依赖专属切换/回滚生命周期。
- 新增脱敏物化器，从原始资源、探针和 HA 事件推导聚合报告、RTO/RPO 和来源 SHA-256；最终证据不保留逐条采样、连接串或目标端点。
- 新增人工目标执行入口、合成适配器合同、`pnpm staging:target-adapter-contract`、统一门禁 `-IncludeStagingAdapters` 和 GitHub Actions 独立作业。
- 同步更新实施计划、下一步开发方案、阶段状态、需求追踪、本地开发说明和 staging 运行手册。
- 合成合同继续强制为 `contract-fixture / not-run`；真实 4 小时 staging、MySQL/Redis HA 切换和四角色审批仍未完成。

## 2026-08-10：WP4 第 15 次迭代——Prometheus 与 HA 平台导出接入

- 新增 `staging-prometheus-range-export-v1` 与 `staging-ha-platform-export-v1`，将规范化 Prometheus 区间序列和托管 MySQL/Redis HA 事件纳入第 14 次迭代通用证据源之前的安全导入层。
- 新增字段白名单、跨序列整秒 UTC 时间对齐、15 秒采样周期、非负/整数指标、HA 模式、执行组和 fixture 防提升校验；原始平台事件仅允许保留 SHA-256 摘要。
- 新增人工平台导出执行入口、合成合同、`pnpm staging:platform-export-contract`、统一门禁与 GitHub Actions 独立作业，并新增 14 项正负向测试；`check.ps1 -SkipInstall -IncludeStagingAdapters` 已通过。
- 同步更新 v3.0 规格、实施计划、下一步开发方案、阶段状态、README 和 staging 运行手册；本地统一门禁通过，远端 CI `31429066132` 因无 runner 分配而无步骤失败；真实 4 小时 staging、HA 切换与四角色签字仍未完成，不得将合同夹具视为目标执行证据。
