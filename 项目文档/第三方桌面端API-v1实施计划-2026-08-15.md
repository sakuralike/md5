# 第三方桌面端 API v1 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task with verification checkpoints.

**Goal:** 为其他开发者提供经过管理员审核、基于系统浏览器 PKCE 授权的第三方桌面端 API，并保持官方桌面端现有验证链路兼容。

**Architecture:** 新增独立的第三方应用注册、用户授权、令牌会话和 Scope 模块；第三方路由不直接复用官方桌面 URL，而是通过第三方 Principal 校验应用状态、用户授权和权限，再复用现有安装实例、挑战、回执、哈希、公告与更新服务。第三方验证回执显式绑定应用并区分普通待验证池和 `desktop:verification:trusted` 总哈希池直入通道。

**Tech Stack:** FastAPI、SQLAlchemy、Alembic、Pydantic、现有 JWT/Refresh Token、Vue 3、TypeScript、Shadcn-Vue、Tailwind、pytest、Vitest、pnpm。

---

## 任务 1：第三方应用与 OAuth 数据模型

**Files:**
- Create: `E:/只比主题/minimax/密码侦探社/apps/api/src/password_detective/db/models/third_party_app.py`
- Create: `E:/只比主题/minimax/密码侦探社/apps/api/src/password_detective/db/models/third_party_oauth.py`
- Modify: `E:/只比主题/minimax/密码侦探社/apps/api/src/password_detective/db/models/__init__.py`
- Create: `E:/只比主题/minimax/密码侦探社/apps/api/alembic/versions/20260815_0040_third_party_apps.py`
- Test: `E:/只比主题/minimax/密码侦探社/apps/api/tests/test_m6_third_party_models.py`

- [x] **Step 1: Write failing model and migration tests**

测试必须验证：

```python
def test_third_party_app_tables_have_future_review_fields(db):
    inspector = inspect(db.get_bind())
    assert {
        "third_party_apps",
        "third_party_app_redirect_uris",
        "third_party_authorizations",
        "third_party_authorization_codes",
        "third_party_token_sessions",
    }.issubset(set(inspector.get_table_names()))
    columns = {column["name"] for column in inspector.get_columns("third_party_apps")}
    assert {"application_source", "status", "trusted_verification_enabled", "reviewer_user_id"} <= columns
```

同时测试 `client_id` 唯一、回调地址唯一、授权码哈希唯一和 Token family 字段存在。

- [x] **Step 2: Run the failing migration test**

```powershell
apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_m6_third_party_models.py -q
```

Expected: FAIL because the tables and model exports do not exist。

- [x] **Step 3: Implement enums, models and migration**

`third_party_apps` 至少包含：`id`、`client_id`、`management_secret_hash`、`name`、`developer_name`、`description`、`status`、`application_source`、`submitted_by_user_id`、`reviewer_user_id`、`reviewed_at`、`review_note`、`trusted_verification_enabled`、`request_count`、`last_used_at`、`last_error_at`、`created_at`、`updated_at`、`revoked_at`。

迁移 `20260815_0040` 的 `down_revision` 固定为 `20260814_0039`，为 MySQL 和 SQLite 测试创建五张表、索引、外键和唯一约束；不修改旧桌面安装实例表，第三方绑定先放在 OAuth/第三方会话表中，避免官方客户端迁移风险。

- [x] **Step 4: Run migration/model tests**

```powershell
apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_m6_third_party_models.py -q
```

Expected: PASS。

- [x] **Step 5: Commit the foundational migration**

```powershell
git add apps/api/src/password_detective/db/models/third_party_app.py apps/api/src/password_detective/db/models/third_party_oauth.py apps/api/src/password_detective/db/models/__init__.py apps/api/alembic/versions/20260815_0040_third_party_apps.py apps/api/tests/test_m6_third_party_models.py
git commit -m "feat: add third-party app oauth models"
```

## 任务 2：管理员第三方应用管理 API

**Files:**
- Create: `E:/只比主题/minimax/密码侦探社/apps/api/src/password_detective/modules/third_party_apps/__init__.py`
- Create: `E:/只比主题/minimax/密码侦探社/apps/api/src/password_detective/modules/third_party_apps/schemas.py`
- Create: `E:/只比主题/minimax/密码侦探社/apps/api/src/password_detective/modules/third_party_apps/service.py`
- Create: `E:/只比主题/minimax/密码侦探社/apps/api/src/password_detective/modules/third_party_apps/router.py`
- Modify: `E:/只比主题/minimax/密码侦探社/apps/api/src/password_detective/main.py`
- Test: `E:/只比主题/minimax/密码侦探社/apps/api/tests/test_m6_third_party_apps_admin.py`

- [x] **Step 1: Write failing Admin API tests**

覆盖以下行为：

```python
def test_admin_can_create_draft_and_approve_third_party_app(client, admin_headers):
    created = client.post("/api/v1/admin/third-party-apps", headers=admin_headers, json={
        "name": "Synthetic Desktop",
        "developer_name": "Synthetic Developer",
        "description": "Synthetic integration",
        "redirect_uris": ["http://127.0.0.1:49152/callback"],
        "scopes": ["profile:read", "hash:read", "desktop:verification"],
    })
    assert created.status_code == 201
    assert created.json()["status"] == "draft"
    assert "management_secret" in created.json()

    app_id = created.json()["id"]
    approved = client.post(f"/api/v1/admin/third-party-apps/{app_id}/approve", headers=admin_headers)
    assert approved.status_code == 200
    assert approved.json()["status"] == "approved"
    assert approved.json()["management_secret"] is None
```

另测：非管理员 403、重复回调地址 422、普通 Scope 不得直接设置可信 Scope、暂停/恢复/撤销写入审计并改变状态、密钥轮换只返回一次新密钥。

- [x] **Step 2: Run tests to verify failure**

```powershell
apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_m6_third_party_apps_admin.py -q
```

Expected: FAIL with 404 because路由尚未注册。

- [x] **Step 3: Implement service and router**

路由：

```text
POST /api/v1/admin/third-party-apps
GET  /api/v1/admin/third-party-apps
GET  /api/v1/admin/third-party-apps/{app_id}
POST /api/v1/admin/third-party-apps/{app_id}/approve
POST /api/v1/admin/third-party-apps/{app_id}/suspend
POST /api/v1/admin/third-party-apps/{app_id}/restore
POST /api/v1/admin/third-party-apps/{app_id}/revoke
POST /api/v1/admin/third-party-apps/{app_id}/rotate-secret
```

统一要求 `require_admin_mfa`；状态变更使用现有审计服务；应用创建生成随机 `client_id` 和一次性管理密钥，数据库只保存密钥哈希。可信 Scope 只能由管理员在审核通过后显式授予，创建请求中的 `desktop:verification:trusted` 必须被拒绝或降级为普通 Scope。

- [x] **Step 4: Run targeted Admin tests and Ruff**

```powershell
apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_m6_third_party_apps_admin.py -q
apps/api/.venv/Scripts/python.exe -m ruff check apps/api/src/password_detective/modules/third_party_apps apps/api/tests/test_m6_third_party_apps_admin.py
```

Expected: PASS。

## 任务 3：PKCE 授权与第三方 Token Principal

**Files:**
- Create: `E:/只比主题/minimax/密码侦探社/apps/api/src/password_detective/modules/third_party_oauth/__init__.py`
- Create: `E:/只比主题/minimax/密码侦探社/apps/api/src/password_detective/modules/third_party_oauth/schemas.py`
- Create: `E:/只比主题/minimax/密码侦探社/apps/api/src/password_detective/modules/third_party_oauth/service.py`
- Create: `E:/只比主题/minimax/密码侦探社/apps/api/src/password_detective/modules/third_party_oauth/dependencies.py`
- Create: `E:/只比主题/minimax/密码侦探社/apps/api/src/password_detective/modules/third_party_oauth/router.py`
- Modify: `E:/只比主题/minimax/密码侦探社/apps/api/src/password_detective/main.py`
- Test: `E:/只比主题/minimax/密码侦探社/apps/api/tests/test_m6_third_party_oauth.py`

- [x] **Step 1: Write failing PKCE tests**

测试授权码一次性使用、S256 PKCE、回调白名单、5 分钟过期、Access Token Scope、Refresh Token 轮换、重放撤销 Token family、应用暂停后令牌失效。

```python
def test_authorization_code_requires_matching_pkce_and_is_single_use(client, approved_app, logged_in_user):
    challenge = base64url(sha256("verifier".encode()))
    issued = authorize_user(client, approved_app, logged_in_user, challenge)
    token = client.post("/api/v1/third-party/oauth/token", json={
        "grant_type": "authorization_code",
        "client_id": approved_app.client_id,
        "code": issued.code,
        "redirect_uri": approved_app.redirect_uri,
        "code_verifier": "verifier",
    })
    assert token.status_code == 200
    replay = client.post("/api/v1/third-party/oauth/token", json={...})
    assert replay.status_code == 400
```

- [x] **Step 2: Run the PKCE tests to verify failure**

```powershell
apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_m6_third_party_oauth.py -q
```

Expected: FAIL with 404 or missing service implementation。

- [x] **Step 3: Implement authorization code and token services**

授权码只保存哈希；PKCE 只接受 `S256`；Access Token 使用专用 audience `third-party-desktop`、15 分钟过期和 Scope claims；Refresh Token 使用哈希、family、轮换和重放撤销。新增 `get_third_party_principal` 和 `require_third_party_scope(scope)`，必须同时校验用户状态、应用状态、授权状态、Token audience 和 Scope。

- [x] **Step 4: Run OAuth tests and security checks**

```powershell
apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_m6_third_party_oauth.py -q
apps/api/.venv/Scripts/python.exe -m ruff check apps/api/src/password_detective/modules/third_party_oauth apps/api/tests/test_m6_third_party_oauth.py
```

Expected: PASS。

**本轮实现记录（2026-08-15）：**

- 已实现 `GET /api/v1/third-party/oauth/authorize`，要求已登录用户、精确回调地址、至少 16 字符 `state` 与 `S256` PKCE。
- 已实现 `POST /api/v1/third-party/oauth/token`，支持授权码交换和 Refresh Token 轮换；授权码 5 分钟过期且原子化单次消费。
- 第三方 Access Token 使用 `third-party-desktop` audience、15 分钟有效期及 Scope claims；Refresh Token 30 天有效并采用 Token family 重放撤销。
- 已实现 `POST /api/v1/third-party/oauth/revoke` 和 `GET /api/v1/third-party/oauth/me`；第三方 Principal 会同时校验用户、应用、授权、Token 会话及 Scope。
- 定向测试覆盖 PKCE、授权码重放与过期、Refresh Token 轮换与重放、Scope 拒绝及应用暂停后令牌失效。

## 任务 4：第三方桌面可信验证适配层

**Files:**
- Create: `E:/只比主题/minimax/密码侦探社/apps/api/src/password_detective/modules/third_party_desktop/__init__.py`
- Create: `E:/只比主题/minimax/密码侦探社/apps/api/src/password_detective/modules/third_party_desktop/schemas.py`
- Create: `E:/只比主题/minimax/密码侦探社/apps/api/src/password_detective/modules/third_party_desktop/router.py`
- Create: `E:/只比主题/minimax/密码侦探社/apps/api/src/password_detective/modules/third_party_desktop/service.py`
- Reuse: `E:/只比主题/minimax/密码侦探社/apps/api/src/password_detective/modules/desktop_verification/service.py`
- Modify: `E:/只比主题/minimax/密码侦探社/apps/api/src/password_detective/db/models/desktop_verification.py`
- Create: `E:/只比主题/minimax/密码侦探社/apps/api/alembic/versions/20260815_0041_third_party_desktop.py`
- Test: `E:/只比主题/minimax/密码侦探社/apps/api/tests/test_m6_third_party_desktop.py`

- [x] **Step 1: Write failing trust-boundary tests**

测试同一成功签名回执在普通 Scope 下进入待验证池，在可信 Scope 下按 `verification-v3` 进入总哈希池；测试挑战和回执不能跨应用复用，缺少 `desktop:verification` 返回 403。

- [x] **Step 2: Run tests to verify failure**

```powershell
apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_m6_third_party_desktop.py -q
```

实际结果：先得到预期的 `ModuleNotFoundError` 红灯，随后进入实现阶段。

- [x] **Step 3: Implement the adapter**

第三方安装、挑战和回执使用独立 `/api/v1/third-party` 路由；挑战记录 app binding；回执 canonical payload 使用 `third-party-desktop-receipt-v1` 并包含 `client_id`。复用现有 ECDSA、候选状态和验证服务，新增 `trust_channel` 判定而不改变官方桌面协议。

- [x] **Step 4: Run targeted tests and existing desktop regression tests**

```powershell
apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_m6_third_party_desktop.py apps/api/tests/test_m2_archive_core.py -q
```

实际结果：第三方桌面端 4/4、官方桌面验证与第三方 OAuth 回归测试通过；Ruff 检查通过。新增 0041 迁移将安装实例、挑战和回执绑定到应用及协议版本。
**本轮实现记录（2026-08-15）：**

- 新增 `POST/GET /api/v1/third-party/installations` 与撤销接口，安装实例记录 `third_party_app_id`、操作系统、架构和第三方回执协议。
- 新增 `POST /api/v1/third-party/challenges` 与 `POST /api/v1/third-party/verification-receipts`；挑战、安装实例、回执均校验当前 OAuth 应用绑定，不能跨应用复用。
- 普通 `desktop:verification` 回执写入 `third_party_pending`，复用候选证据服务但不允许直接升级总哈希池；管理员批准且 Token 同时包含 `desktop:verification:trusted` 的应用写入 `third_party_trusted` 并按现有验证规则进入已验证池。
- 第三方 canonical payload 固定为 `third-party-desktop-receipt-v1` 并纳入 `client_id`；官方桌面端继续使用原有 `desktop-receipt-v1` 字段默认值和路由。

## 任务 5：只读 API、Web 授权页和 Admin 工作区

**Files:**
- Create: `E:/只比主题/minimax/密码侦探社/apps/web/src/pages/ThirdPartyAuthorizePage.vue`
- Create: `E:/只比主题/minimax/密码侦探社/apps/web/src/pages/AuthorizedApplicationsPage.vue`
- Create: `E:/只比主题/minimax/密码侦探社/apps/web/src/services/thirdPartyApps.ts`
- Create: `E:/只比主题/minimax/密码侦探社/apps/web/src/pages/ThirdPartyAuthorizePage.test.ts`
- Create: `E:/只比主题/minimax/密码侦探社/apps/web/src/pages/AuthorizedApplicationsPage.test.ts`
- Create: `E:/只比主题/minimax/密码侦探社/apps/admin/src/pages/ThirdPartyAppsPage.vue`
- Create: `E:/只比主题/minimax/密码侦探社/apps/admin/src/pages/ThirdPartyAppsPage.test.ts`
- Modify: `E:/只比主题/minimax/密码侦探社/apps/web/src/router/index.ts`
- Modify: `E:/只比主题/minimax/密码侦探社/apps/admin/src/router/index.ts`
- Modify: `E:/只比主题/minimax/密码侦探社/packages/api-contract/src/index.ts`

- [x] **Step 1: Write failing Web/Admin rendering and service tests**

覆盖授权详情、Scope 展示、同意/拒绝、已授权应用撤销、Admin 创建/审核/暂停/可信权限按钮和 API 参数序列化。

- [x] **Step 2: Run Web/Admin tests to verify failure**

```powershell
pnpm --dir apps/web test -- --run src/pages/ThirdPartyAuthorizePage.test.ts src/pages/AuthorizedApplicationsPage.test.ts
pnpm --dir apps/admin test -- --run src/pages/ThirdPartyAppsPage.test.ts
```

Expected: FAIL because pages, routes and services do not exist。

- [x] **Step 3: Implement Shadcn/Tailwind pages and contracts**

页面使用 `<script setup lang="ts">`、明确接口类型、Shadcn-Vue 组件和 Tailwind 工具类；不新增自定义 `<style>`、行内样式或其他 UI 库。所有成功/失败反馈在页面保留，不能在刷新列表时丢失。

- [x] **Step 4: Run Web/Admin lint, typecheck and tests**

```powershell
pnpm --dir apps/web lint
pnpm --dir apps/web typecheck
pnpm --dir apps/web test -- --run
pnpm --dir apps/admin lint
pnpm --dir apps/admin typecheck
pnpm --dir apps/admin test -- --run
```

Expected: PASS。

## 任务 6：开发者文档、合约和全量门禁

**Files:**
- Create: `E:/只比主题/minimax/密码侦探社/项目文档/第三方桌面端API-v1开发者文档.md`
- Modify: `E:/只比主题/minimax/密码侦探社/packages/api-contract/src/index.ts`
- Modify: `E:/只比主题/minimax/密码侦探社/项目文档/文档变更记录.md`
- Modify: `E:/只比主题/minimax/密码侦探社/项目文档/密码侦探社后续开发计划-2026-08-13.md`

- [ ] **Step 1: Add stable API contracts and synthetic examples**

文档必须包含 PKCE、Scope、错误码、令牌轮换、签名 canonical payload、Python/C#/TypeScript 示例；示例只能使用合成数据。

- [ ] **Step 2: Run final verification**

```powershell
pwsh ./scripts/check.ps1 -SkipInstall
```

Expected: `All checks passed.`

- [ ] **Step 3: Commit implementation**

```powershell
git diff --check
git status --short
git add <only-third-party-files>
git commit -m "feat: add third-party desktop api v1"
```

必须保留未提交的 `AGENTS.md` 和 `易语言桌面ui.e`。

## 任务 7：远端推送、Staging 部署与实际验收

**Files:**
- Create: `E:/只比主题/minimax/密码侦探社/.local/deploy-<revision>.sh`
- Create: `E:/只比主题/minimax/密码侦探社/.local/staging-third-party-api-verification.log`

- [ ] **Step 1: Push the exact local tree**

使用现有 `.local/sync_git_data_api.py`，确认远端分支 `codex/m5-entry-gates` 的 commit tree 与本地 `HEAD^{tree}` 一致，并记录远端提交。

- [ ] **Step 2: Build and deploy verified artifacts**

生成 API、Web、Admin 制品，记录 SHA-256；使用现有 Staging 发布脚本模式部署，确认迁移、API 2、Worker 3、Scheduler、Web、Admin 全部健康。

- [ ] **Step 3: Execute real acceptance**

使用合成第三方应用和合成用户数据验证：

1. 未审核应用拒绝授权。
2. 已审核应用 PKCE 授权成功。
3. 授权码重放失败。
4. Scope 缺失返回 403。
5. 普通第三方成功回执进入待验证池。
6. 可信 Scope 成功回执进入总哈希池。
7. 应用暂停后 Token 和 API 调用失效。
8. Web/Admin/API 入口返回预期状态码。

不得把真实密码、令牌、密钥或个人信息写入仓库、日志或文档。

### 本轮实现记录（2026-08-15）

- Web 新增 `/oauth/authorize` 授权确认页，展示应用身份、回调地址和 Scope，支持同意/拒绝并将结果安全回跳到第三方回调地址。
- Web 新增 `/account/authorized-applications`，展示已授权应用、Scope、授权时间和最近使用时间，支持用户撤销授权；撤销时同步吊销对应第三方 Token 会话。
- API 新增 OAuth consent 详情、同意/拒绝、已授权应用列表和用户撤销接口；保留原有 `/third-party/oauth/authorize` 直接跳转协议兼容性。
- Admin 新增 `/third-party-apps` 应用治理工作台，支持管理员手动创建、审核、授予可信验证直入 Scope、暂停、恢复和撤销应用，为后续开发者自助申请保留 `application_source` 状态字段。
- `packages/api-contract` 补充第三方应用、OAuth 授权确认和已授权应用稳定 TypeScript 合同；新增 SSR 渲染测试覆盖 Web/Admin 核心入口。
- 定向结果：Web 58/58、Admin 84/84；Web/Admin typecheck 与 lint 通过；第三方 OAuth API 测试 7/7（含授权确认、已授权应用列表与撤销授权），Ruff 通过。
